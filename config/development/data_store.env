# 数据存储配置 - Development

# 路径由 composition root 的 DITTO_STATE_ROOT 显式注入。
# SQLITE_PATH / DUCKDB_PATH / LOG_DIR 仅保留一个迁移版本的环境变量兼容；
# 不要在配置文件中声明部署路径。

# SQL 引擎配置
SQL_ENGINE__ENABLE_PLAN_CACHE=true
SQL_ENGINE__PLAN_CACHE_SIZE=1000
SQL_ENGINE__SLOW_QUERY_THRESHOLD=1.0
