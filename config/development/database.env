# 数据库配置 - Development
# DB_* 前缀的配置
# 注意：DatabaseSettings 目前只使用 computed_field（duckdb_path, sqlite_path）
# 这些路径由 XDG Base Directory 规范自动管理
# 如果未来需要添加配置项，可以使用 DB_ 前缀

# 示例配置（当前未使用）：
# DB_CONNECTION_POOL_SIZE=10
# DB_MAX_OVERFLOW=20
