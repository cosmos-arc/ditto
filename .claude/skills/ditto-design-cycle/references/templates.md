# 输出模板

> 审查过程中的 审查输出格式、冲突协调格式和最终报告模板。

---

## 审查输出格式

每个审查角色的输出必须遵循以下结构（采用 Sadler 三段式反馈：标准 + 现状 + 修复方案）：

```markdown
## [角色名] 审查报告

### P0 — 必须修复

#### [P0] UI-001: 主标题字号偏小

**标准**: spec 定义 hero h1 应使用 text-display-lg (28px)，与页面层级 L1 标题一致。
**现状**: `.hero h1` 实际字号 24px，视觉层次偏弱，标题不够突出。
**方案对比**:
| 方案 | 业界参考 | 优势 | 劣势 |
|------|---------|------|------|
| A: 改为 28px | Linear / Vercel | 与 spec 一致，视觉层次清晰 | 需调整下方内容间距 |
| B: 保持 24px | — | 不影响现有布局 | 与 spec 不符，跨页不一致 |

**推荐**: 方案 A，与 spec 对齐是最高优先级，间距可通过 grid 调整吸收。

#### [P0] ...

### P1 — 建议修复

#### [P1] UI-002: 卡片间距不统一

**现状**: `.card-grid` 的 gap 为 16px，但其他页面同类布局使用 20px。
**影响**: 跨页面视觉节奏不一致。

**方案对比**:
| 方案 | 业界参考 | 优势 | 劣势 |
|------|---------|------|------|
| A: gap → 20px | Raycast / shadcn | 与项目其他页面一致 | 略减少可显示卡片数 |
| B: 保持 16px | — | 信息密度更高 | 跨页不一致 |

**推荐**: 方案 A，一致性优先于单页信息密度。

#### [P1] ...

### P2 — 可选优化
| ID | 问题 | 位置 | 建议 | 理由 |
|----|------|------|------|------|

### 💡 设计建议（可能涉及信息架构/交互流程调整）
- 建议将"市场脉搏"从数据表格改为卡片式展示，理由是...
- 建议增加"快速操作"入口，理由是...

### 📋 状态覆盖完整度（仅含 State Coverage Index 的原型）

> 当 Phase 1 BASELINE 解析到 State Coverage Index 时，每个角色必须额外检查。
> 三区架构原型：检查 states-gallery / overlays-gallery 中 gallery-card 覆盖情况。

| 检查项 | 状态 | 问题 |
|--------|------|------|
| Tab 面板全部渲染（default-view 内） | ✅/⚠️/❌ | {空壳 tab 列表} |
| Overlay 画廊覆盖蓝图定义（overlays-gallery） | ✅/⚠️/❌ | {缺失的 overlay 卡片} |
| 状态画廊覆盖（states-gallery） | ✅/⚠️/❌ | {缺失的组件组/卡片} |
| Gallery card label 匹配 Index | ✅/⚠️/❌ | {label 不匹配的卡片} |
| 推理内容标注 [⚠️ 待 PM 定义] | ✅/⚠️ | {需确认的内容列表} |
| Zone 切换可交互（radio 导航） | ✅/⚠️/❌ | {无法切换 zone} |
| HTML 结构完整性（section 平衡） | ✅/⚠️/❌ | {section 开/关标签不匹配} |
| 无 HTML 实体转义（`&lt;` 等） | ✅/⚠️/❌ | {转义位置} |
| Overlay trigger 在 section 外（body 层级） | ✅/⚠️/❌ | {被 section 包裹的 trigger} |

**状态覆盖率**: {已渲染 gallery-card 数}/{Index 声明总数} = {百分比}%

> **HTML 结构验证规则**（防止 BeautifulSoup/lxml 序列化损坏和批量迁移丢失）:
> - `&lt;` `&gt;` `&amp;` 等实体不应出现在标签位置（仅内容文本中合法）
> - `<section>` 开/关标签数量必须匹配
> - `<input class="overlay-radio">` 必须在所有 `<section>` 外（body 直系子元素）
> - `class="overlay-backdrop"` 必须在 `id="overlays-gallery"` section 内
> - `.gallery-grid` 直接子元素只能是 `.gallery-group` 或 `.gallery-card`（禁止 `.main-content` / `.activity-stack` 等非 gallery 元素嵌套）
> - `overlays-gallery` 中每个 `.gallery-card` 必须含可渲染弹层内容（`.overlay-sheet` / `.overlay-drawer` / `.toast-card`），不允许空预览区或纯占位文本
> - 迁移场景：迁移前 state-variant/overlay 总数 vs 迁移后 gallery-card 总数，不允许减少
> - 批量 HTML 变更后必须运行结构验证脚本（用 DOM 解析器，禁止正则）

**角色专项状态检查**（各角色按自身关注点补充）:
- **[角色名]**: {本角色关注的状态问题，如 UX 关注 empty state 的 CTA 是否合理、IA 关注 gallery 中组件分组逻辑是否正确等}
```

> **业界调研规则**: 仅 P0、P1 问题附带方案对比。通过 网页检索 查找 2-3 个业界参考（如 Linear、Raycast、Bloomberg 等），形成轻量对比表格。P2 和纯建议不附带调研。

---

## 冲突协调格式

```markdown
## 冲突协调

### 🔀 冲突 1: 间距 vs 信息密度
- **UI Designer**: 增大卡片间距到 24px，提升呼吸感
- **Product Manager**: 保持 16px 间距，在有限空间展示更多数据
- **分析**: 当前页面面向专业交易用户，信息密度优先级高于呼吸感
- **推荐方案**: 保持 16px 间距，但增加卡片内部分区的视觉分隔（细线或背景色差）
- **用户决策**: [待选择]

### ✅ 共识点
- 所有人认为导航栏需要增加"快捷操作"入口
- 所有人认为空状态需要优化
```

---

## 最终报告模板

输出到 `docs/reviews/YYYY-MM-DD-product-review-{page}.md`

```markdown
# Product Review: {page}

**日期**: YYYY-MM-DD
**目标**: {file path}
**质量等级**: {functional | good | polished | best}
**审查角色**: UI / UX / Product / Copy / Art Director
**模式**: {人工模式 | 自主迭代（goal: X.X, max-rounds: N）}

## 版本信息
- **Tag**: review/round-{N}
- **变更查看**: `git diff review/round-{N-1}..review/round-{N}`

## Summary
- P0 问题: X 个（已修复 X 个）
- P1 问题: X 个（采纳 X 个）
- P2 建议: X 个（采纳 X 个）
- 设计决策变更: X 个
- Lighthouse 评分: {before} → {after}

## 状态覆盖报告

> 仅当原型包含 State Coverage Index 时输出此章节。
> 三区架构：gallery-card 覆盖情况 + Zone 切换可用性。

| 类别 | 总数 | 已渲染 | 覆盖率 | 状态 |
|------|------|--------|--------|------|
| Tab 面板（default-view 内） | N | N | N% | ✅/⚠️ |
| Overlay（overlays-gallery 卡片） | N | N | N% | ✅/⚠️ |
| 状态卡片（states-gallery 卡片） | N | N | N% | ✅/⚠️ |
| Gallery card label 匹配 Index | N | N | N% | ✅/⚠️ |
| **合计** | **N** | **N** | **N%** | **✅/⚠️** |

**Zone 切换可用性**: default-view / states-gallery / overlays-gallery 三个 radio tab 均可正常切换: ✅/⚠️/❌

**待 PM 确认内容**: {标注 [⚠️ 待 PM 定义] 的内容列表}

**状态相关问题**:
- {P0/P1 级别的状态覆盖问题}

## 迭代历程（仅 --iterate 模式）

| 轮次 | 创意策略 | 综合分 | P0 | 采纳建议 | 关键改动 | 关键洞察 | 状态 |
|------|---------|--------|----|---------|---------|---------|------|
| R1 | 基础修补 | 7.6 | 3→0 | 12 | 修正 Token 一致性、调整间距节奏 | 基础问题清零后才能做方向性优化 | 继续 |
| R2 | 定向精修 | 7.9 | 0 | 5 | 精修色彩层次、优化字体排版 | 材质感提升对高级感维度最有效 | 达标 ✓ |
| R3 | — | — | — | — | 未触发（R2 已达标） | — | — |

**评分快照**:
┌─────────────┬─────────┬─────────┬─────────┬─────────┐
│ 指标         │ Round 1 │ Round 2 │ Round 3 │ 趋势     │
├─────────────┼─────────┼─────────┼─────────┼─────────┤
│ 克制度       │  7.8    │  8.2    │  —      │  ↑      │
│ 一致性       │  7.5    │  7.8    │  —      │  ↑      │
│ 高级感       │  7.0    │  7.6    │  —      │  ↑      │
│ 品牌方向     │  8.0    │  8.1    │  —      │  →      │
│ 综合气质     │  7.6    │  7.9    │  —      │  ↑      │
│ P0 残留      │  3      │  0      │  —      │  ✓      │
│ 视觉指纹超标 │  2      │  0      │  —      │  ✓      │
│ Lighthouse   │ 82/90   │ 88/95   │  —      │  ↑      │
│ 状态         │ 继续    │ 达标 ✓  │  —      │         │
└─────────────┴─────────┴─────────┴─────────┴─────────┘

**自动裁决记录**（仅 --iterate 模式）:
- R1-Conflict-01: UI 想加大间距 vs PM 想保持密度 → AD 裁决：保持密度，用内部视觉分隔补偿
- R1-Conflict-02: UX 要求更大的触摸目标 vs AD 克制要求 → UX 胜（可访问性不妥协）
- R1-C-Class-01: [分歧简述] → 分歧等级: 低/中/高 → PM 裁决: [结果] → 依据: [调研/分析/标杆参考]

**反思记录**（仅 --iterate 模式）:

> 从 Reflexion 模式借鉴，每轮记录定性洞察，供后续轮次的 CREATIVE DIRECTION 参考。

**Round 1 反思**

| 字段 | 内容 |
|------|------|
| 创意策略 | 基础修补 |
| 实际执行 | 修正 Token 一致性、调整间距节奏、修复对比度 |
| 效果 | 综合分 7.6（↑），基础问题清零 |
| 关键洞察 | Token 对齐对一致性维度贡献最大；间距梯度调整对信息效率有间接提升 |
| 死胡同 | — |
| 可探索 | 材质感提升（backdrop blur / noise texture）可能对高级感维度有效 |

**Round 2 反思**

| 字段 | 内容 |
|------|------|
| 创意策略 | 定向精修（聚焦高级感维度） |
| 实际执行 | 色彩层次精修、字体排版优化、微交互反馈 |
| 效果 | 综合分 7.9（↑），高级感维度 +0.6 |
| 关键洞察 | 材质感提升（backdrop blur）对高级感最有效，但需控制在 AD 阈值内 |
| 死胡同 | 纯装饰性 glow 效果被 AD 否决（克制度降分） |
| 可探索 | 数据可视化可能对信息效率维度有较大提升空间 |

## 气质评分卡（Art Director）

| 维度 | 评分 | 说明 |
|------|------|------|
| 克制度 | X.X/10 | {高亮描边密度、视觉元素层级数是否在阈值内} |
| 一致性 | X.X/10 | {跨页视觉语言是否统一，有无方言分裂} |
| 高级感 | X.X/10 | {整体气质偏向 Bloomberg/quant desk 的程度} |
| 品牌方向 | X.X/10 | {是否符合 Graphite Studio 审美方向} |
| **综合气质** | **X.X/10** | |

**视觉指纹对比**（Phase 1 基线 → Phase 7 最终）:
| 指标 | 基线 | 最终 | 变化 |
|------|------|------|------|
| 高亮描边密度 | X | X | +/-X |
| 强调色面积比 | X% | X% | +/-X% |
| 视觉元素层级数 | X | X | +/-X |
| 留白节奏比 | X% | X% | +/-X% |
| 色彩种类数 | X | X | +/-X |

**Art Director 裁决记录**:
- {描述降级/移除的 polish 变更及理由}

## 跨页一致性

> 仅在 Edition manifest 存在时输出此章节。

| 指标 | 本页值 | Edition 基线 | 偏离 | 状态 |
|------|--------|-------------|------|------|
| table header height | Xpx | Xpx | +/-Xpx | 一致/偏离 |
| card padding | Xpx | Xpx | +/-Xpx | 一致/偏离 |
| badge size | Xpx | Xpx | +/-Xpx | 一致/偏离 |
| button height | Xpx | Xpx | +/-Xpx | 一致/偏离 |
| h1 font-size | Xpx | Xpx | +/-Xpx | 一致/偏离 |
| font-family-* 4-role | heading/body/data/code 各司其职 | — | 违规 N 处 | 一致/偏离 |
| tabular-nums | data 组件已启用 | — | 缺失 N 处 | 一致/偏离 |
| brand accent count | N | N | +/-N | 一致/偏离 |
| surface elevation 步数 | N | N | +/-N | 一致/偏离 |

**偏离项**: X 项（P0: X, P1: X）
**manifest 更新**: styleAnchor 链路 → 下一页将以本页为 reference

## 视口验证

| 视口 | 分辨率 | 内容完整 | 截断(px) | 可滚动 | sticky 正常 | 状态 |
|------|--------|---------|---------|--------|------------|------|
| VP-STANDARD | 1536x1080 | ✓/✗ | N | Y/N | Y/N | 通过/失败 |
| VP-COMPACT | 1366x768 | ✓/✗ | N | Y/N | Y/N | 通过/失败 |

**body overflow**: {当前值}
**截断修复**: {如有修复，描述方案}

## Key Decisions
- 决策 1: {描述} → {最终方案} → 理由
- 决策 2: ...

## Changes Made

### 累计变更清单（多轮合并）
| 轮次 | ID | 类型 | 描述 |
|------|----|----|------|
| R1 | UI-001 | P0 | 主标题字号对齐 spec |
| R1 | UX-003 | P1 | 增大按钮触摸区域 |
| R2 | AD-002 | P1 | 降低卡片描边对比度 |

## Updated Specs
- {spec file}: {变更描述}

## 待同步清单

> 以下变更已通过验收，可使用 `$ditto-design-cycle {file} --sync` 同步到设计文档。

### {spec 文档路径}
| # | 变更类型 | 描述 | 来源 |
|---|---------|------|------|
| 1 | 修正/新增/补充 | {变更描述} | {P0/P1 ID 或 Conflict ID} |

### docs/designs/decisions/
| # | 变更类型 | 描述 | 来源 |
|---|---------|------|------|
| 1 | 新增 | ADR: {决策标题} | {来源} |

## Screenshots
- 无自动截图（需要时在浏览器手动截取）
```

---

## ESCALATE 模板（高分歧产品决策升级）

> 当 PM 深度分析流程判定为高分歧时，使用此模板呈现给用户。
> 格式：宿主原生用户提问能力 选项 + 结构化分析文本。

### 宿主原生用户提问能力 选项

```
问题: "以下产品决策存在高分歧，需要你的裁决。"

选项:
  ✅ 接受 PM 推荐方案 — [PM 推荐方案的一句话摘要]
  ❌ 否决 — 保持现状，不执行该变更
  🔄 替代方案 — [其他角色提出的方案，如有]
```

### 附带分析文本

```markdown
## ⚠️ 产品决策升级 — 需要你的裁决

### [分歧严重度标签] 分歧 N: [一句话描述分歧核心]

**分歧严重度**: 🔴 方向性 / 🟡 重要权衡 / 🟢 轻微越界
**升级原因**: [多角色对立 / PM 无法判断 / 方向性选择]
**来源**: Round {N} Phase 3/4

#### 各角色立场

| 角色 | 立场 | 核心理由 | Spec 依据 |
|------|------|---------|-----------|
| PM | [立场] | [理由] | [有/无/间接参考] |
| AD | [立场] | [理由] | [有/无/间接参考] |
| IA | [立场] | [理由] | [有/无/间接参考] |
| UX | [立场] | [理由] | [有/无/间接参考] |

#### PM 推荐方案

**推荐**: [方案描述]
**理由**: [PM 的分析逻辑，2-3 句，基于分歧分析和调研]

#### 业界调研参考

| 标杆产品 | 如何处理 | 对我们的启示 |
|---------|---------|-------------|
| [产品 A] | [具体做法] | [适用/不适用 + 原因] |
| [产品 B] | [具体做法] | [适用/不适用 + 原因] |

#### 决策后果预测

| 选项 | 正面影响 | 负面影响/风险 |
|------|---------|-------------|
| ✅ 接受 PM 推荐 | [具体收益] | [具体风险] |
| ❌ 否决 | [保持现状的合理性] | [错失的优化机会] |
| 🔄 替代方案 | [替代方案收益] | [替代方案风险] |

#### [--iterate] 待确认变更

PM 推荐方案已先行执行，标记为「待确认」。
可通过以下命令查看变更或回滚：
- 查看变更: `git diff review/<task>/round-{N-1}..review/<task>/round-{N}`
- 回滚变更: `git checkout review/<task>/round-{N-1} -- <file>`
```

---

## 分歧分析记录模板

> 用于 REFLECT 阶段记录当轮的 C 类分歧处理情况，纳入反思记录。

```markdown
### Round {N} C 类分歧记录

#### 分歧 1: [简述]
- **等级**: 低/中/高
- **类型**: spec 缺失 / 功能增删 / 方向性选择 / 优先级冲突
- **各角色立场**: PM [立场] vs AD [立场] vs ...
- **业界调研**: [标杆 A] [做法], [标杆 B] [做法]
- **裁决结果**: PM 裁决 [方案] / ESCALATE 给用户 [用户选择]
- **裁决依据**: [调研发现 + 分析逻辑]
- **后续影响**: [对设计/分数的影响]
```
