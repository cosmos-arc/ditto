# Cross-Market contract feedback

- Retained the radar shell, context bar, scope strip, main scan area, and right rail.
- The public surface currently supports instrument metadata, not cross-market prices, correlations, events, or capital flows. The page therefore scans actual identity coverage by asset class and exchange.
- The old bottom tab band and five prototype-only overlays were omitted because they depended on unavailable market-evidence contracts.
- The frozen prototype lets its evidence grid grow beyond the viewport; the live page keeps the public four-row catalog scan inside the application viewport. Page-specific geometry thresholds record that reviewed intrinsic-height difference while selector, error, and target parity remain zero-tolerance.
