[project]
name = "ditto-apps"
requires-python = ">= 3.13"
version = "0.1.0"

[project.scripts]
server = "ditto_apps.main:app"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
