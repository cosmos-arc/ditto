<b>{{ strategy_name | default(strategy_id) }}</b> — 调仓信号 [{{ level | upper }}]
{{ signal_date }}{% if regime_label is defined %} | Regime: {{ regime_label }}{% if regime_score is defined %} ({{ regime_score }}){% endif %}{% endif %}

{% for action in actions %}
{{ "📈" if action.action == "buy" else "📉" }} <b>{{ action.instrument_id }}</b>
  {{ action.action | upper }} | 权重 {{ action.current_weight }}→{{ action.target_weight }}
  Δ {{ action.delta_weight }}
{% endfor %}
{% if timestamp %}<i>{{ timestamp }}</i>{% endif %}
