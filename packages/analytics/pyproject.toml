[project]
name = "ditto-analytics"
requires-python = ">= 3.13"
version = "0.1.0"
dependencies = [
    "ditto-data",
    "ditto-kernel",
    "polars",
    "numpy",
    "cachebox",
    "orjson",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"
