# R1 · G1 Evidence Package — 2026-07-16

> Decision: **G1 PASS — internal local Beta**.
>
> R1 Tasks 1–8、Task 9 的确定性、恢复、真实数据、前端和 loopback
> 证据全部完成。获批的 `lupa` 运行时修复使 default/dev Prefect 服务均在
> `127.0.0.1:4200` 通过实际健康检查；`/api/v1/trade` 因此按 Gate 规则提升为
> `initial-focus`。本 Gate 不授权公网、商业、自动交易或真实券商生产使用。

## 1. Run identity

| Field | Value |
|---|---|
| Evidence timestamp | 2026-07-16 21:05:16 Asia/Shanghai |
| Backend branch / implementation HEAD | `feature/r1-development` / `36c95059372aade090bc8ae641c4866bf216733b` |
| Backend evidence scope | implementation HEAD plus the maturity/docs promotion containing this package |
| Frontend branch / HEAD | `feature/r1-trading-live` / `5814cddf676e3fafca28e391132c36ee3420961a` |
| Frontend worktree | dirty with pre-existing/unrelated user changes; the R1 commit was staged from an explicit whitelist |
| Operator | Codex local acceptance |
| Deterministic signal date / D+1 | `2026-07-10` / `2026-07-13` |
| Live signal date / D+1 | `2024-03-29` / `2024-04-01` |
| Seed / published version | `seed_stock_selection_rotation` / `1` |
| Accounts | `r1-paper`, `real-paper` (non-sensitive paper aliases) |
| Live provider | Tushare; credential value omitted |

## 2. G1 completion matrix

| # | Required proof | Result | Evidence |
|---|---|---|---|
| 1 | Idempotent seed bootstrap and latest-published selection | PASS | Deterministic E2E proves `published -> unchanged`; a newer draft cannot shadow the active published version. |
| 2 | Atomic account/position baseline and one stable sleeve | PASS | Baseline validation, atomic rollback, idempotent replay, explicit correction audit, and query tests pass. |
| 3 | D data produces explainable D+1 sizing | PASS | `2026-07-10 -> 2026-07-13`; `600519` BUY quantity `4000`, including lot/cash/reference-price evidence. |
| 4 | Trade and zero-rebalance signal packages | PASS | Both deterministic package revisions persist with checksum and explicit `no_rebalance` semantics. |
| 5 | Same-input no-op and changed-input fail-closed | PASS | Stable artifact/intent identity, `RERUN_CONFLICT` after fill, and interrupted-finalize convergence are covered. |
| 6 | Multiple partial fills and append-only correction | PASS | Two original partial fills, one replacement fill, one replace adjustment, and one void adjustment; raw/effective/adjustment identities survive backup/restore. |
| 7 | Daily Decision V2 truth table | PASS | Fixed precedence, ownership/isolation, checksum, overfill, correction, and reason-code tests pass. |
| 8 | `ditto-app` live path without prototype fallback | PASS | `VITE_USE_MOCK=false`; 14/14 desktop/mobile states include multi-fill ledger and fill correction. |
| 9 | CLI and Prefect share one coordinator | PASS | CLI and Prefect both delegate to `run_eod_pipeline`; Prefect orchestration unit tests pass without entering a second business pipeline. |
| 10 | SQLite backup/restore and operator runbook | PASS | Online WAL backup, independent restore, integrity, row counts, decision equality, and no temporary sidecar leak are proven. |
| 11 | Backend/frontend/full architecture gates and deterministic E2E | PASS | Final backend HEAD, committed frontend snapshot, architecture contracts, deterministic E2E, and explicit real-data acceptance pass. |
| 12 | Explicit real-data evidence package | PASS | Fresh Tushare acceptance completed with no skips; credential is not copied into logs or this package. |
| 13 | Unauthenticated services are loopback-only | PASS | Static configuration, actual API runtime, and default/dev Prefect runtime all bind only to loopback and shut down cleanly. |

All thirteen rows pass; no row is waived or replaced by the aggregate score.

## 3. Deterministic R1 and Task 6 evidence

Commands:

```bash
pixi run -e dev pytest \
  packages/apps/tests/e2e/test_r1_daily_manual_trading.py \
  --no-cov -q -n0

pixi run -e dev pytest \
  packages/apps/tests/unit/test_loopback_binding_unit.py \
  packages/apps/tests/e2e/test_r1_daily_manual_trading.py \
  --no-cov -q -n0
```

Fresh results:

- deterministic R1 E2E: **7 passed**;
- combined loopback static guard plus R1 E2E: **13 passed**;
- independent Task 6 focused review suite: **1,019 passed**, with zero review
  blockers or findings.

Task 6 immutable identities:

| View | IDs |
|---|---|
| Raw fills | `task6-partial-1`, `task6-partial-2`, `task6-partial-1-corrected` |
| Effective fills | `task6-partial-1-corrected` |
| Adjustments | `task6-adjust-replace`, `task6-adjust-void` |

The replacement is appended as a new fill and linked by a replace adjustment.
The second partial fill is made ineffective by a void adjustment. No original
fill row is overwritten or deleted. Because the system is not deployed, the
approved R1 schema is a fresh-schema contract: no legacy compatibility,
historical backfill, or historical rollback path is required.

## 4. Backup and restore evidence

The final WAL scenario uses the official backup/restore services and
`ops verify-sqlite`. A regression test also asserts that no temporary database,
`-wal`, `-shm`, or `-journal` artifact containing `.partial` remains.

Fresh result:

| Database | Integrity | SHA-256 | Fills | Adjustments |
|---|---|---|---:|---:|
| source logical database | `ok` | main-file hash not compared while WAL is active | 3 | 2 |
| backup | `ok` | `sha256:8f75b21b336edc2392126ae3d3709a637bb600a6a403401ba95adb7766fde98c` | 3 | 2 |
| restored | `ok` | `sha256:8f75b21b336edc2392126ae3d3709a637bb600a6a403401ba95adb7766fde98c` | 3 | 2 |

All source/backup/restored business-table row counts are equal. The source main
file alone has a different byte hash because committed pages are still in its
active WAL; SQLite read-only verification observes the full logical database.
The backup and restored standalone files are byte-identical. The restored Daily
Decision response and Task 6 raw/effective/adjustment identities equal the
source response.

The cleanup regression and backup/restore E2E pass together: **7 passed**.
The implementation fix is committed as:

```text
f55deac9 fix: clean sqlite backup sidecars
```

## 5. Explicit real-data acceptance

Command (credential supplied only by the operator environment/keyring):

```bash
DITTO_RUN_REAL_DATA_ACCEPTANCE=1 \
pixi run -e dev pytest \
  packages/apps/tests/e2e/test_real_data_stock_selection_pipeline.py \
  -m e2e --no-cov -q -n0
```

Fresh result: **2 passed in 35.07s; 0 skipped**.

| Evidence | Value | Result |
|---|---|---|
| Published seed | `seed_stock_selection_rotation / 1 / published` | PASS |
| Signal date / intended date | `2024-03-29 / 2024-04-01` | PASS |
| Required datasets | stock daily, adjustment factor, balance sheet, income statement all ready with persisted quality evidence | PASS |
| Stable replay | same package/checksum/intent identities on the second official EOD CLI run | PASS |
| Daily Decision | persisted package is queryable; quantity-unavailable state is explicit review, not a fabricated order | PASS |
| Credential hygiene | token never printed, persisted, or copied into this package | PASS |

## 6. Frontend live acceptance

The acceptance runner exercises React with `VITE_USE_MOCK=false` against
explicit HTTP fixtures. It proves the real frontend adapter path, reason-code
rendering, interaction guards, responsive behavior, and absence of the
prototype/MSW fallback. Backend business behavior is proven separately by the
API and E2E tests above.

R1 frontend evidence:

- R1 branch commit: `5814cdd feat: complete R1 live trading workflow`;
- 14/14 screenshots: seven states at `1536x900` and `390x844`;
- states: `blocked`, `review`, `review-fills`, `ready`,
  `fill-review`, `multi-fill-ledger`, and `fill-correction`;
- exact committed-index snapshot: TypeScript passed; Vitest **165 files /
  1,926 tests**;
- exact committed-index production build passed after generating the ignored
  TanStack route tree; only Vite's non-blocking chunk-size warning remained;
- broader current mixed-worktree check: Biome **546 files**, TypeScript passed,
  Vitest **165 files / 1,931 tests**;
- independent review: Critical 0, Major 0; 14/14 acceptance and contrast
  **175 checks / 0 failures**.

The mixed-worktree result is recorded only as supplementary evidence because
that frontend worktree contains unrelated user changes. The committed R1 scope
was reviewed and committed from explicit paths/hunks; no broad staging or
cleanup was used.

Evidence directory:

```text
/home/chevy/projects/ditto-app/docs/review/r1-trading-acceptance/
```

## 7. Final backend quality and architecture gates

Backend implementation HEAD `36c95059372aade090bc8ae641c4866bf216733b`
plus the promotion changes containing this package:

| Gate | Fresh result | Result |
|---|---|---|
| Ruff lint | clean | PASS |
| Ruff format | 1,877 files unchanged | PASS |
| basedpyright | 0 errors, 0 warnings, 0 notes | PASS |
| Fast/full default suite | 8,936 passed, 1 documented xfailed in 37.06s | PASS |
| Import contracts | 37 kept, 0 broken; 1,017 files / 3,312 dependencies analyzed | PASS |
| Architecture smells | clean | PASS |
| Full pre-commit | all hooks passed | PASS |
| Deterministic R1 E2E | 7 passed | PASS |
| Real-data Tushare acceptance | 2 passed, 0 skipped | PASS |

The one xfail is the pre-existing documented Polars nested
cross-section/time-series limitation. It is unrelated to R1 and was neither
introduced nor hidden by this work.

## 8. Loopback runtime proof

Static loopback guards pass for direct application entrypoints, Pixi tasks,
Docker/observability host publications, active ops documentation, and
`DITTO_DATA_ROOT=/app/data`.

Actual API runtime proof:

- `pixi run server` started all four Granian workers;
- `GET http://127.0.0.1:8000/healthz` returned HTTP 200;
- live `/openapi.json` exposed both the Daily Decision V2 operation and the
  `trade` tag as `x-ditto-maturity: initial-focus`;
- `ss` showed only `127.0.0.1:8000` for all API workers;
- shutdown completed cleanly with no lingering API process.

Approved dependency repair:

```toml
lupa = ">=2.1,<3"
```

Pixi resolved `lupa 2.8` with Lua/LuaJIT for default/dev on both Linux and
Windows. A static regression guard asserts the explicit manifest dependency.

Actual runtime command, executed independently in default and dev:

```bash
pixi run prefect server start \
  --host 127.0.0.1 --port 4200 --no-services --no-ui

pixi run -e dev prefect server start \
  --host 127.0.0.1 --port 4200 --no-services --no-ui
```

For both runs:

- `GET http://127.0.0.1:4200/api/health` returned `true`;
- `ss` showed exactly `127.0.0.1:4200`, never a wildcard/LAN listener;
- Ctrl-C returned exit 0 with `Server stopped!`;
- the socket was absent after shutdown.

Dependency/runtime fix commit:

```text
36c95059 fix: install Prefect Lua runtime
```

## 9. Final decision

- G1 result: **PASS**
- Completed rows: **13 / 13**
- Maturity: `/api/v1/trade = initial-focus`
- Capability breadth / R1 readiness: **5.9 / 7.9**
- Product boundary: internal local Beta, not public,
  commercial, automated, or broker-production trading
