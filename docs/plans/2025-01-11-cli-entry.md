# CLI 入口实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 为 ditto 项目创建 CLI 入口，功能完全对标 ingestion 数据摄取功能

**架构:** 重构 `apps/server` → `apps/port`，CLI 作为 port 的子模块 (`apps/port/src/ditto_port/cli/`)，通过入口点暴露，本地执行无需 Prefect Server

**Tech Stack:** Typer (CLI 框架), IngestionCoordinator (核心逻辑), BackfillManager (回补功能)

**重要变更:** 本计划包含 Phase 0 重构任务，将 `apps/server` 重命名为 `apps/port`，包名从 `ditto_port` 改为 `ditto_port`

---

## Sprint/Phase 关联

- **Sprint**: Sprint 2 (数据层完善与验证)
- **Phase**: Phase 6 (CLI 入口实现)
- **创建时间**: 2025-01-11

---

## 需求确认

| 维度 | 选择 |
|------|------|
| 使用场景 | 混合模式（参数优先 + 交互可选） |
| 执行方式 | 本地执行（无需 Prefect Server） |
| 命令结构 | 按数据集分类 |
| 额外功能 | 仅核心摄取 |

---

## 风险识别

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Phase 0 重构影响现有测试 | 高 | 每个子任务后验证测试通过，增量提交 |
| 包名重命名导致 import 错误 | 高 | 使用编辑器全局替换 + 验证 |
| Typer 与现有依赖冲突 | 中 | 先验证 pixi 环境兼容性 |
| CLI 执行器与 Prefect Flow 重复代码 | 中 | 确保共享 services 层逻辑 |
| CLI 冷启动性能不达标 | 低 | 使用缓存/延迟加载优化 |

---

## PIT 处理说明

CLI 涉及的数据摄取操作统一通过 `IngestionCoordinator` 和 `BackfillManager` 执行，PIT（Point-In-Time）语义由 services 层统一处理，CLI 不直接操作 Store。

## 命令示例

```bash
ditto stock daily --date 2024-01-02 [--force]
ditto stock backfill --start 2024-01-01 --end 2024-01-31 [--parallel 4]
ditto etf daily --date 2024-01-02 [--force]
ditto calendar
ditto stock basic
ditto etf basic
```

---

## 目录结构

```
apps/port/                       # 统一入口层（原 server）
└── src/ditto_port/
    ├── __init__.py
    ├── main.py                 # FastAPI 应用
    │
    ├── api/                    # HTTP API 入口
    │   ├── __init__.py
    │   └── routes/             # FastAPI 路由
    │
    ├── cli/                    # CLI 入口
    │   ├── __init__.py
    │   ├── main.py             # Typer 应用入口
    │   ├── executor.py         # 本地执行器
    │   ├── commands/           # 命令实现
    │   │   ├── __init__.py
    │   │   ├── stock.py        # Stock 相关命令
    │   │   ├── etf.py          # ETF 相关命令
    │   │   ├── calendar.py     # Calendar 命令
    │   │   └── adj.py          # 复权因子命令
    │   └── utils/              # CLI 工具函数
    │       ├── __init__.py
    │       ├── output.py       # 输出格式化
    │       └── validation.py   # 参数验证
    │
    ├── jobs/                   # 定时任务入口
    │   ├── __init__.py
    │   ├── flows/              # Prefect Flow 定义（调用 services）
    │   │   ├── daily.py
    │   │   ├── backfill.py
    │   │   ├── repair.py
    │   │   └── tasks/          # Prefect Task 定义
    │   └── deploy.py           # Flow 部署脚本
    │
    └── services/               # 业务服务层（无框架依赖，被 api/cli/jobs 调用）
        ├── __init__.py
        └── ingestion/          # 摄取服务
            ├── __init__.py
            ├── coordinator.py  # 摄取协调器
            ├── backfill.py     # 回补管理器
            └── config/         # 配置
                ├── datasets.py # 数据集注册表
                └── config.py
```

---

## 任务清单

**任务复杂度说明**：
- `S`: 简单任务（< 2 小时）
- `M`: 中等任务（2-4 小时）
- `L`: 复杂任务（4-8 小时）

---

### Phase 0: server → port 重构 + 目录重组

**说明:** 在开始 CLI 实现之前，先完成：
1. `apps/server` → `apps/port` 重命名
2. `ditto_port` → `ditto_port` 包名重命名
3. 目录重组：分离业务服务和 Prefect 组件
   - `services/ingestion/` ← 业务逻辑
   - `jobs/flows/` ← Prefect 组件

---

**Task 0.1: 目录重命名 apps/server → apps/port** `[M]`

**验收标准**:
- [ ] `apps/server` 目录已重命名为 `apps/port`
- [ ] pyproject.toml 包名已更新为 `ditto-port`
- [ ] git mv 命令已正确执行
- [ ] commit 已创建

**测试要求**: 无需测试（仅目录重命名）

**Step 1: 使用 git mv 重命名目录**

```bash
git mv apps/server apps/port
```

**Step 2: 更新 pyproject.toml 包名**

修改 `apps/port/pyproject.toml`:

```toml
[project]
name = "ditto-port"  # 原 ditto-server
```

**Step 3: Commit**

```bash
git add apps/port/pyproject.toml
git commit -m "refactor: rename apps/server to apps/port"
```

---

**Task 0.2: 包名重命名 ditto_port → ditto_port** `[L]`

**验收标准**:
- [ ] `ditto_port` 目录已重命名为 `ditto_port`
- [ ] pyproject.toml 包名和入口点已更新
- [ ] 所有 import 语句已批量更新
- [ ] pixi.toml 中的引用已更新
- [ ] 文档中的引用已更新
- [ ] 所有测试通过 (`pixi run -e dev pytest apps/port/tests/ -v`)
- [ ] commit 已创建

**测试要求**:
- @pytest.mark.unit
- @pytest.mark.integration
- 重构后覆盖率保持 >= 80%

**Step 1: 重命名 src 目录**

```bash
git mv apps/port/src/ditto_port apps/port/src/ditto_port
```

**Step 2: 更新 pyproject.toml**

修改 `apps/port/pyproject.toml`:

```toml
[project]
name = "ditto-port"
packages = [{include = "ditto_port", from = "src"}]

[project.scripts]
server = "ditto_port.main:app"
```

**Step 3: 批量更新所有 import 语句**

使用编辑器全局替换：
- `from ditto_port` → `from ditto_port`
- `import ditto_port` → `import ditto_port`

**Step 4: 更新 pixi.toml 中的引用**

搜索并更新所有 `ditto-server` → `ditto-port`。

**Step 5: 更新文档**

搜索 `docs/` 目录，更新所有 `ditto_port` → `ditto_port` 的引用。

**Step 6: 验证测试**

```bash
pixi run -e dev pytest apps/port/tests/ -v
```

**Step 7: Commit**

```bash
git add apps/port/
git commit -m "refactor: rename package ditto_port to ditto_port"
```

---

**Task 0.3: 目录重组 - 分离业务服务和 Prefect 组件**

**Step 1: 创建 services/ 和 jobs/ 目录**

```bash
mkdir -p apps/port/src/ditto_port/services
mkdir -p apps/port/src/ditto_port/jobs/flows
mkdir -p apps/port/src/ditto_port/jobs/tasks
```

**Step 2: 移动业务服务到 services/ingestion/**

```bash
# 创建 ingestion 子目录
mkdir -p apps/port/src/ditto_port/services/ingestion

# 移动业务逻辑（非 Prefect 组件）
git mv apps/port/src/ditto_port/ingestion/services apps/port/src/ditto_port/services/ingestion/
git mv apps/port/src/ditto_port/ingestion/config apps/port/src/ditto_port/services/ingestion/
```

**Step 3: 移动 Prefect 组件到 jobs/**

```bash
# 移动 flows
git mv apps/port/src/ditto_port/ingestion/flows/* apps/port/src/ditto_port/jobs/flows/

# 移动 tasks
git mv apps/port/src/ditto_port/ingestion/tasks/* apps/port/src/ditto_port/jobs/tasks/

# 删除空的 ingestion 目录
rmdir apps/port/src/ditto_port/ingestion/flows
rmdir apps/port/src/ditto_port/ingestion/tasks
rmdir apps/port/src/ditto_port/ingestion
```

**Step 4: 更新所有 import 语句**

使用编辑器全局替换：
- `from ditto_port.ingestion.services` → `from ditto_port.services.ingestion`
- `from ditto_port.ingestion.flows` → `from ditto_port.jobs.flows`
- `from ditto_port.ingestion.tasks` → `from ditto_port.jobs.tasks`
- `from ditto_port.ingestion.config` → `from ditto_port.services.ingestion.config`

**Step 5: 创建 services/__init__.py**

```python
# apps/port/src/ditto_port/services/__init__.py
"""业务服务层（无框架依赖），被 api/cli/jobs 调用。"""

from ditto_port.services import ingestion

__all__ = ["ingestion"]
```

**Step 6: 创建 services/ingestion/__init__.py**

```python
# apps/port/src/ditto_port/services/ingestion/__init__.py
"""摄取业务服务。"""

from ditto_port.services.ingestion.coordinator import IngestionCoordinator
from ditto_port.services.ingestion.backfill import BackfillManager

__all__ = ["IngestionCoordinator", "BackfillManager"]
```

**Step 7: 创建 jobs/__init__.py**

```python
# apps/port/src/ditto_port/jobs/__init__.py
"""定时任务入口（Prefect 相关）。"""

__all__ = []
```

**Step 8: 验证测试**

```bash
pixi run -e dev pytest apps/port/tests/ -v
```

**Step 9: Commit**

```bash
git add apps/port/src/ditto_port/services/ apps/port/src/ditto_port/jobs/
git commit -m "refactor: separate business services and Prefect components"
```

---

**Task 0.4: 验证重构**

**Step 1: 验证导入**

```bash
python -c "from ditto_port import __version__; print(__version__)"
```

**Step 2: 验证 FastAPI 服务**

```bash
pixi run server --help
```

**Step 3: 运行完整测试套件**

```bash
pixi run -e dev pytest apps/port/tests/ -v
```

**Step 4: Commit (如有修复)**

---

### Phase 1: 基础设施

**Task 1.1: 创建 CLI 目录结构**

**Files:**
- Create: `apps/port/src/ditto_port/cli/__init__.py`
- Create: `apps/port/src/ditto_port/cli/commands/__init__.py`
- Create: `apps/port/src/ditto_port/cli/utils/__init__.py`

**Step 1: 创建目录和 __init__.py 文件**

```bash
mkdir -p apps/port/src/ditto_port/cli/commands
mkdir -p apps/port/src/ditto_port/cli/utils
```

**Step 2: 配置入口点**

修改 `apps/port/pyproject.toml`，添加：

```toml
[project.scripts]
ditto = "ditto_port.cli.main:app"
```

**Step 3: Commit**

```bash
git add apps/port/pyproject.toml apps/port/src/ditto_port/cli/
git commit -m "feat: create CLI directory structure and entry point"
```

---

### Phase 2: Executor 核心执行器

**Task 2.1: 实现 CLIExecutor 类**

**Files:**
- Create: `apps/port/src/ditto_port/cli/executor.py`
- Test: `apps/port/tests/unit/cli/test_executor.py`

**Step 1: 编写失败的测试**

```python
# test_executor.py
import pytest
from unittest.mock import Mock
from ditto_port.cli.executor import CLIExecutor

@pytest.mark.unit
def test_executor_init(app_ctx):
    """测试 CLIExecutor 初始化"""
    mock_coordinator = Mock()
    mock_backfill = Mock()

    # 测试初始化不抛出异常
    executor = CLIExecutor(app_ctx)
    assert executor is not None
```

**Step 2: 运行测试验证失败**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_executor.py::test_executor_init -v
```

**Step 3: 实现最小实现**

```python
# executor.py
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from ditto_foundation.app_initializer import AppContext

class CLIExecutor:
    """CLI 本地执行器，封装 IngestionCoordinator 和 BackfillManager"""

    def __init__(self, app_ctx: "AppContext") -> None:
        self._app_ctx = app_ctx
        self._hub = app_ctx.hub
        self._source = app_ctx.source

        from ditto_port.services.ingestion.coordinator import IngestionCoordinator
        from ditto_port.services.ingestion.backfill import BackfillManager

        self._coordinator = IngestionCoordinator(
            hub=self._hub,
            source=self._source,
        )

        self._backfill_manager = BackfillManager(
            coordinator=self._coordinator,
            calendar_store=self._hub.calendar_store,
            ingestion_log_store=self._hub.ingestion_log,
        )

    def ingest_daily(self, dataset: str, trade_date: str, force: bool = False) -> dict:
        """执行单日摄取"""
        result = self._coordinator.ingest_date(dataset, trade_date, force)
        return {
            "dataset": dataset,
            "trade_date": trade_date,
            "status": result.status,
            "row_count": result.row_count,
            "message": result.message,
            "error": result.error,
        }

    def backfill_range(self, dataset: str, start_date: str, end_date: str, parallel: int = 1) -> dict:
        """执行日期范围回补"""
        result = self._backfill_manager.backfill_range(
            dataset=dataset,
            start_date=start_date,
            end_date=end_date,
            parallel=parallel,
        )
        return {
            "dataset": dataset,
            "total_dates": result.total_dates,
            "success_count": result.success_count,
            "skipped_count": result.skipped_count,
            "failed_count": result.failed_count,
        }
```

**Step 4: 运行测试验证通过**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_executor.py -v
```

**Step 5: Commit**

```bash
git add apps/port/src/ditto_port/cli/executor.py apps/port/tests/unit/cli/test_executor.py
git commit -m "feat: implement CLIExecutor with ingest_daily and backfill_range"
```

---

### Phase 3: Utils 工具函数

**Task 3.1: 实现参数验证**

**Files:**
- Create: `apps/port/src/ditto_port/cli/utils/validation.py`
- Test: `apps/port/tests/unit/cli/test_validation.py`

**Step 1: 编写失败的测试**

```python
# test_validation.py
import pytest
import typer
from ditto_port.cli.utils.validation import validate_date_format

@pytest.mark.unit
def test_validate_date_format_valid():
    """测试有效日期格式"""
    validate_date_format("2024-01-02")  # 不应抛出异常

@pytest.mark.unit
def test_validate_date_format_invalid():
    """测试无效日期格式"""
    with pytest.raises(typer.Exit):
        validate_date_format("2024/01/02")
```

**Step 2: 运行测试验证失败**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_validation.py -v
```

**Step 3: 实现验证逻辑**

```python
# validation.py
import re
from datetime import datetime
import typer

DATE_PATTERN = re.compile(r"^\d{4}-\d{2}-\d{2}$")

def validate_date_format(date_str: str) -> None:
    """验证日期格式 (YYYY-MM-DD)"""
    if not DATE_PATTERN.match(date_str):
        typer.echo(f"错误: 日期格式应为 YYYY-MM-DD, 收到: {date_str}")
        raise typer.Exit(1)

    try:
        datetime.strptime(date_str, "%Y-%m-%d")
    except ValueError:
        typer.echo(f"错误: 无效日期: {date_str}")
        raise typer.Exit(1)
```

**Step 4: 运行测试验证通过**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_validation.py -v
```

**Step 5: Commit**

```bash
git add apps/port/src/ditto_port/cli/utils/validation.py apps/port/tests/unit/cli/test_validation.py
git commit -m "feat: add date format validation utility"
```

---

**Task 3.2: 实现输出格式化**

**Files:**
- Create: `apps/port/src/ditto_port/cli/utils/output.py`
- Test: `apps/port/tests/unit/cli/test_output.py`

**Step 1: 编写失败的测试**

```python
# test_output.py
import pytest
from ditto_port.cli.utils.output import print_ingestion_result

@pytest.mark.unit
def test_print_ingestion_result_success(capsys):
    """测试成功结果输出"""
    result = {
        "dataset": "stock_daily",
        "trade_date": "2024-01-02",
        "status": "success",
        "row_count": 5000,
    }
    print_ingestion_result(result, verbose=False)
    captured = capsys.readouterr()
    assert "success" in captured.out
    assert "5000" in captured.out
```

**Step 2: 运行测试验证失败**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_output.py -v
```

**Step 3: 实现输出格式化**

```python
# output.py
import typer
from typing import Any

def print_ingestion_result(result: dict[str, Any], verbose: bool = False) -> None:
    """打印摄取结果"""
    status = result["status"]
    status_color = {
        "success": typer.colors.GREEN,
        "skipped": typer.colors.YELLOW,
        "failed": typer.colors.RED,
    }.get(status, typer.colors.WHITE)

    typer.secho(f"状态: {status}", fg=status_color, bold=True)
    typer.echo(f"数据集: {result['dataset']}")

    if result.get("trade_date"):
        typer.echo(f"交易日期: {result['trade_date']}")

    if result.get("row_count"):
        typer.echo(f"行数: {result['row_count']}")

    if verbose and result.get("message"):
        typer.echo(f"消息: {result['message']}")

    if result.get("error"):
        typer.secho(f"错误: {result['error']}", fg=typer.colors.RED)

def print_backfill_summary(result: dict[str, Any]) -> None:
    """打印回补摘要"""
    typer.secho("回补完成", fg=typer.colors.GREEN, bold=True)
    typer.echo(f"数据集: {result['dataset']}")
    typer.echo(f"总日期数: {result['total_dates']}")
    typer.echo(f"成功: {result['success_count']}")
    typer.echo(f"跳过: {result['skipped_count']}")

    if result['failed_count'] > 0:
        typer.secho(f"失败: {result['failed_count']}", fg=typer.colors.RED)
```

**Step 4: 运行测试验证通过**

```bash
pixi run -e dev pytest apps/port/tests/unit/cli/test_output.py -v
```

**Step 5: Commit**

```bash
git add apps/port/src/ditto_port/cli/utils/output.py apps/port/tests/unit/cli/test_output.py
git commit -m "feat: add output formatting utilities"
```

---

### Phase 4: Stock 命令组

**Task 4.1: 实现 stock daily 命令**

**Files:**
- Create: `apps/port/src/ditto_port/cli/commands/stock.py`
- Test: `apps/port/tests/integration/cli/test_stock_commands.py`

**Step 1: 编写失败的测试**

```python
# test_stock_commands.py
import pytest
from typer.testing import CliRunner
from ditto_port.cli.main import app

runner = CliRunner()

@pytest.mark.integration
def test_stock_daily_command(test_settings_session):
    """测试 stock daily 命令"""
    result = runner.invoke(app, ["stock", "daily", "2024-01-02"])
    assert result.exit_code == 0
    assert "stock_daily" in result.stdout
```

**Step 2: 运行测试验证失败**

```bash
pixi run -e dev pytest apps/port/tests/integration/cli/test_stock_commands.py::test_stock_daily_command -v
```

**Step 3: 实现命令逻辑**

```python
# stock.py
import typer
from ditto_port.cli.utils.output import print_ingestion_result
from ditto_port.cli.utils.validation import validate_date_format

app = typer.Typer(help="股票数据摄取命令")

@app.command()
def daily(
    ctx: typer.Context,
    date: str = typer.Argument(..., help="交易日期 (YYYY-MM-DD)"),
    force: bool = typer.Option(False, "--force", "-f", help="强制重新摄取"),
) -> None:
    """摄取股票日行情数据"""
    validate_date_format(date)

    executor = ctx.obj["executor"]
    result = executor.ingest_daily("stock_daily", date, force)

    print_ingestion_result(result, ctx.obj["verbose"])
```

**Step 4: 运行测试验证通过**

```bash
pixi run -e dev pytest apps/port/tests/integration/cli/test_stock_commands.py::test_stock_daily_command -v
```

**Step 5: Commit**

```bash
git add apps/port/src/ditto_port/cli/commands/stock.py apps/port/tests/integration/cli/test_stock_commands.py
git commit -m "feat: implement stock daily command"
```

---

**Task 4.2: 实现 stock backfill 命令**

**Step 1: 编写失败的测试**

```python
@pytest.mark.integration
def test_stock_backfill_command(test_settings_session):
    """测试 stock backfill 命令"""
    result = runner.invoke(app, ["stock", "backfill", "--start", "2024-01-01", "--end", "2024-01-05"])
    assert result.exit_code == 0
```

**Step 2: 运行测试验证失败**

**Step 3: 实现命令逻辑**

```python
# 添加到 stock.py
@app.command()
def backfill(
    ctx: typer.Context,
    start: str = typer.Option(..., "--start", "-s", help="开始日期 (YYYY-MM-DD)"),
    end: str = typer.Option(..., "--end", "-e", help="结束日期 (YYYY-MM-DD)"),
    parallel: int = typer.Option(1, "--parallel", "-p", help="并行度 (默认 1)"),
) -> None:
    """回补股票历史数据"""
    validate_date_format(start)
    validate_date_format(end)

    executor = ctx.obj["executor"]
    result = executor.backfill_range("stock_daily", start, end, parallel)

    print_backfill_summary(result)
```

**Step 4: 运行测试验证通过**

**Step 5: Commit**

```bash
git add apps/port/src/ditto_port/cli/commands/stock.py
git commit -m "feat: implement stock backfill command"
```

---

**Task 4.3: 实现 stock basic 命令**

**Step 1: 编写测试**

**Step 2: 实现命令**

```python
# 添加到 stock.py
@app.command()
def basic(
    ctx: typer.Context,
    force: bool = typer.Option(False, "--force", "-f", help="强制重新摄取"),
) -> None:
    """摄取股票基础信息"""
    executor = ctx.obj["executor"]
    result = executor.ingest_daily("stock_basic", "", force)

    print_ingestion_result(result, ctx.obj["verbose"])
```

**Step 3: Commit**

```bash
git add apps/port/src/ditto_port/cli/commands/stock.py
git commit -m "feat: implement stock basic command"
```

---

### Phase 5: 其他命令组

**Task 5.1: 实现 ETF 命令组**

**Files:**
- Create: `apps/port/src/ditto_port/cli/commands/etf.py`

参考 stock.py 实现：
- `etf daily --date <date>`
- `etf backfill --start <date> --end <date>`
- `etf basic`

**Commit:**

```bash
git add apps/port/src/ditto_port/cli/commands/etf.py
git commit -m "feat: implement ETF command group"
```

---

**Task 5.2: 实现 Calendar 命令**

**Files:**
- Create: `apps/port/src/ditto_port/cli/commands/calendar.py`

```python
import typer
from ditto_port.cli.utils.output import print_ingestion_result

app = typer.Typer(help="交易日历命令")

@app.command()
def default(
    ctx: typer.Context,
    force: bool = typer.Option(False, "--force", "-f", help="强制重新摄取"),
) -> None:
    """更新交易日历"""
    executor = ctx.obj["executor"]
    result = executor.ingest_daily("calendar", "", force)

    print_ingestion_result(result, ctx.obj["verbose"])
```

**Commit:**

```bash
git add apps/port/src/ditto_port/cli/commands/calendar.py
git commit -m "feat: implement calendar command"
```

---

**Task 5.3: 实现复权因子命令**

**Files:**
- Create: `apps/port/src/ditto_port/cli/commands/adj.py`

实现 `adj-factor` 和 `fund-adj` 命令。

**Commit:**

```bash
git add apps/port/src/ditto_port/cli/commands/adj.py
git commit -m "feat: implement adj-factor and fund-adj commands"
```

---

### Phase 6: 主入口与集成

**Task 6.1: 实现 main.py 入口**

**Files:**
- Create: `apps/port/src/ditto_port/cli/main.py`
- Modify: `apps/port/src/ditto_port/cli/commands/__init__.py`

**Step 1: 创建主入口**

```python
# main.py
import typer
from ditto_foundation import initialize_app
from ditto_port.cli.executor import CLIExecutor
from ditto_port.cli.commands import stock, etf, calendar, adj

app = typer.Typer(
    name="ditto",
    help="Ditto 量化系统命令行工具",
    no_args_is_help=True,
    add_completion=True,
)

# 注册命令组
app.add_typer(stock.app, name="stock")
app.add_typer(etf.app, name="etf")
app.add_typer(calendar.app, name="calendar")
app.add_typer(adj.app, name="adj")

@app.callback()
def main(
    ctx: typer.Context,
    data_root: str = typer.Option(None, "--data-root", "-d", help="数据根目录"),
    verbose: bool = typer.Option(False, "--verbose", "-v", help="详细输出模式")
) -> None:
    """初始化 CLI 上下文"""
    ctx.ensure_object(dict)

    # 初始化应用
    app_ctx = initialize_app(
        service_name="ditto-cli",
        data_root=data_root,
    )

    # 创建执行器
    executor = CLIExecutor(app_ctx)

    ctx.obj["executor"] = executor
    ctx.obj["verbose"] = verbose
    ctx.obj["app_ctx"] = app_ctx

@app.command()
def version() -> None:
    """显示版本信息"""
    typer.echo("ditto-cli v0.1.0")

if __name__ == "__main__":
    app()
```

**Step 2: 更新 commands/__init__.py**

```python
from ditto_port.cli.commands import stock, etf, calendar, adj

__all__ = ["stock", "etf", "calendar", "adj"]
```

**Step 3: 测试入口点**

```bash
pixi run ditto --help
pixi run ditto --version
```

**Step 4: Commit**

```bash
git add apps/port/src/ditto_port/cli/main.py apps/port/src/ditto_port/cli/commands/__init__.py
git commit -m "feat: implement CLI main entry point"
```

---

**Task 6.2: 端到端集成测试**

**Files:**
- Create: `apps/port/tests/integration/cli/test_e2e.py`

**Step 1: 编写端到端测试**

```python
# test_e2e.py
import pytest
from typer.testing import CliRunner
from ditto_port.cli.main import app

runner = CliRunner()

@pytest.mark.integration
def test_e2e_stock_daily_workflow(test_settings_session):
    """测试完整的 stock daily 工作流"""
    # 测试帮助
    result = runner.invoke(app, ["stock", "daily", "--help"])
    assert result.exit_code == 0

    # 测试执行
    result = runner.invoke(app, ["stock", "daily", "2024-01-02"])
    assert result.exit_code == 0
    assert "stock_daily" in result.stdout

@pytest.mark.integration
def test_e2e_backfill_workflow(test_settings_session):
    """测试完整的 backfill 工作流"""
    result = runner.invoke(app, [
        "stock", "backfill",
        "--start", "2024-01-01",
        "--end", "2024-01-03",
        "--parallel", "2"
    ])
    assert result.exit_code == 0
```

**Step 2: 运行测试**

```bash
pixi run -e dev pytest apps/port/tests/integration/cli/test_e2e.py -v
```

**Step 3: Commit**

```bash
git add apps/port/tests/integration/cli/test_e2e.py
git commit -m "test: add end-to-end integration tests"
```

---

### Phase 7: 文档与优化

**Task 7.1: 创建 CLI 文档**

**Files:**
- Create: `docs/cli/README.md`

**内容:**

```markdown
# Ditto CLI 使用指南

## 安装

CLI 随 ditto-port 一起安装：

\`\`\`bash
pixi install
\`\`\`

## 快速开始

### 摄取单日数据

\`\`\`bash
ditto stock daily --date 2024-01-02
ditto etf daily --date 2024-01-02
\`\`\`

### 回补历史数据

\`\`\`bash
ditto stock backfill --start 2024-01-01 --end 2024-01-31
ditto etf backfill --start 2024-01-01 --end 2024-01-31 --parallel 4
\`\`\`

### 更新基础信息

\`\`\`bash
ditto stock basic
ditto etf basic
ditto calendar
\`\`\`

## 命令参考

[...]
```

**Commit:**

```bash
git add docs/cli/README.md
git commit -m "docs: add CLI usage guide"
```

---

**Task 7.2: 验证覆盖率**

**Step 1: 运行覆盖率检查**

```bash
pixi run -e dev pytest apps/port/tests/cli/ --cov=apps/port/src/ditto_port/cli --cov-report=term
```

**Step 2: 确保覆盖率 ≥ 80%**

如果低于 80%，补充测试用例。

**Step 3: Commit (如果需要)**

---

## 关键文件

- `apps/port/src/ditto_port/cli/main.py` - CLI 应用入口
- `apps/port/src/ditto_port/cli/executor.py` - 核心执行器
- `apps/port/src/ditto_port/cli/commands/stock.py` - Stock 命令组（模板）
- `apps/port/src/ditto_port/services/ingestion/coordinator.py` - 现有协调器
- `apps/port/src/ditto_port/services/ingestion/config/datasets.py` - 数据集配置

## 验证步骤

1. **安装验证**
   ```bash
   pixi run ditto --help
   ```

2. **单元测试**
   ```bash
   pixi run -e dev pytest -m unit apps/port/tests/cli/
   ```

3. **集成测试**
   ```bash
   pixi run -e dev pytest -m integration apps/port/tests/cli/
   ```

4. **覆盖率检查**
   ```bash
   pixi run -e dev pytest apps/port/tests/cli/ --cov=apps/port/src/ditto_port/cli --cov-report=term
   ```

5. **端到端测试**
   ```bash
   pixi run ditto stock daily --date 2024-01-02
   pixi run ditto stock backfill --start 2024-01-01 --end 2024-01-05
   ```

## 成功标准

- [ ] 所有计划命令可正常执行
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 集成测试覆盖核心流程
- [ ] CLI 响应时间 < 2 秒（冷启动）
- [ ] 错误消息友好可读
- [ ] 帮助信息完整清晰
