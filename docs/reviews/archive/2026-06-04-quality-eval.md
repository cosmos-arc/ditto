# Ditto 质量评估报告 2026-06-04

> 评估模式: --quick | 评估时间: 2026-06-04 | 项目阶段: 架构整改完成，V1 功能开发中
> Remediation update 2026-06-09: SQLitePool 多线程 `close_all()` 确定性测试已改为 worker 上报 + Event 释放模型，避免 fast thread exit / thread-id reuse / SQLite WAL 初始化并发时序导致的 flaky；`SQLiteClient.count()` 已加入表名与受约束参数化 WHERE 片段校验，PLAT-P1-01 关闭；application/backtest root `__all__` 已补齐并新增全包 root public API guard，`docs/architecture/public-api.md` 已新增 12 包 root stable export manifest 并由 drift guard 精确匹配当前 `__all__`；post-ingest catalog/write/process paths 已引入 `CatalogWriteContext` / `DataWriteContext` / `PostIngestContext`，instrument runtime/post-process/backfill 已引入 `InstrumentIngestContext` / `InstrumentPostIngestContext` / `InstrumentBackfillContext`，ingestion composition root 已引入 `CoordinatorRuntimeContext`，materialization run record assembly 已引入 `MaterializationRunRecordContext`，materialized data persistence 已引入 `MaterializedDataPersistenceContext`，publication safety persistence 已引入 `PublicationSafetyPersistenceContext`，materialization runtime ports 已引入 `MaterializationRuntimePorts`，materialization provider composition 已拆为 artifact writer / source snapshot resolver / governance ports / runtime ports providers，features artifact metadata update 已引入 `ArtifactMetadataUpdateParams`，backtest engine result assembly 已引入 `EngineResultAssemblyContext`，data metadata identity resolver 已引入 `IdentityResolverContext` / `IdentityResolutionRequest`，`MetadataProvider.metadata_service` 已拆为 domain/runtime dependency bundles，`MetadataService` 构造入口已收敛为 `MetadataServiceDeps` 并保留 legacy dependency bridge，`MetadataService.find_securities` 已收敛为 `SecurityQuery` / legacy filter bridge，`EngineLoop` runtime collaborators 已收敛为 `EngineLoopDeps` 并保留 legacy constructor bridge，apps backtest job flow wire parameters 已收敛为 `BacktestFlowRequest` 并保留 keyword wire compatibility，`MarketService.list_bars` 已收敛为 `MarketBarsQuery` request-object API 并保留 legacy keyword bridge，`DerivedArtifactReader.read_frame` 已收敛为 `DerivedArtifactFrameRequest` 并保留 legacy keyword bridge；源码行内 C6 `PLR0913` suppressions 降至 2，独立 `ruff --isolated --select PLR0913` 生产扫描全通过，2 个生产行内 suppressions 均为 apps CLI/Typer 框架入口且已由 architecture guard 固化为受控例外，0 个未受控生产 C6 debt；execution broker reconnect/replay 已新增首条长 callback 序列 conformance fixture，覆盖 first-session connect/ack、reconnected connect、重复 fill replay 幂等、cancel 和 account_update 的 broker-order link 传播；broker-event conformance sink 已对齐 SQLite `event_time + insertion order` 语义，锁住 submit ack 先于 immediate `fill_query_error` 的失败排序证据，并新增同 run 多 broker `broker_order_id` 查询过滤 fixture，证明 alpha broker order ID 不会返回 beta broker events；`BrokerGatewayDescriptor` 已把 gateway identity、contract version、capability declarations 和 standard event types 纳入 conformance；apps FastAPI OpenAPI `operationId` 已切到 tag-scoped stable ids，避免独立前端生成客户端被路径/HTTP suffix 污染，并由 `TestOpenAPIMaturity` 锁定唯一性；import-linter stale ignore 已清理，apps-service-isolation unmatched ignore 从 warn 收紧为 error；apps E2E reporter 纯单测不再被 session autouse 数据校验误跳过；最新 full check 为 8257 passed, 1 xfailed, 0 failed。
> Backend contract update: source-health summary 的 `attention_required` item 已补齐 `namespace`、`default_source` 与 `selected_source_health`，让后端消费者可直接定位 catalog 资产命名空间并解释默认源偏离、fallback、selected source freshness/storage/schema 证据和 governance attention；该工作仅扩展 FastAPI/application DTO 合同，不触碰产品 UI。
> Backend contract update: source-health summary 已补齐 backend-owned `attention_severity` 与固定顺序 `attention_severity_counts`，把 selected source missing/stale/not_applicable、promotion revoked、default failover/no fallback、unsupported source 等 reason 收敛为 critical/warning/info triage 合同，避免独立前端复制告警优先级规则。
> Backend contract update: 单条 source-health report 已补齐顶层 `selected_source_health`、`source_selection_status` 与 `source_selection_blockers`，让 `GET /ingestion/catalog/source-health` 与 summary 明细保持一致，并让 unsupported selected source 明确表现为 blocked 后端 DTO 证据；该工作不实现真实 source/broker adapter，也不处理产品 UI。
> Backend contract update: source-health summary 已补齐固定顺序 `source_selection_status_counts`，并把 `source_selection_status` / `source_selection_blockers` 透传到 `attention_required` item；后端消费者可直接从 summary 识别 blocked 选源覆盖和具体 blocker，不需要遍历明细 report 或复制 source-selection policy。
> Backend orchestration update: `source=auto` 在 catalog freshness 选源后、委托 concrete coordinator 前复用 dataset source-capability guard；当 selected source 不支持当前 dataset 时，date-level、range-level 与 instrument-level ingestion 都会以结构化 `AppProcessError` fail closed，逐日/按请求选择携带 `operation` / `selection_date`，无日期枚举器的 range fallback 携带 `operation` / `start_date` / `end_date`，且不会调用具体 coordinator。该工作只收紧 application 编排边界，不新增真实 source/broker adapter，也不处理产品 UI。
> Backend orchestration update: instrument-level `source=auto` 的多日 `start_date/end_date` 请求现在复用普通 ingestion date schedule，逐日按 catalog freshness/SLA 选源、逐日委托 source-consistent coordinator，并聚合 backend `IngestionResult`；单日或日期不完整的 instrument 请求继续按请求日期选源。该切片只补后端 FastAPI/application 服务的编排合同，不新增真实券商/数据源 adapter，也不触碰独立前端产品 UI。
> Backend remediation update: catalog remediation source-health backlog/detail item 已透传 `source_selection_status` / `source_selection_blockers`；当 selected source blocked 时不再暴露 `repair_catalog_source_coverage` write intent，而是只提供 fallback 配置与 source request 复核类 manual intents，避免后端审批合同给出会被 source-capability guard fail-closed 的误导性自动修复路径；该工作不接真实券商、不实现产品 UI。
> Backend remediation update: `IngestDateHandler` 现在把 process-layer structured details 保真映射到 `AppCommandError`，catalog remediation approval-backed execution 也会把所有 registered executable action 抛出的 typed `AppCommandError` 转为 `status="failed"` execution DTO、保留 `error_type/error/details` 并补充 approval_id/item_id/action execution context，追加 `execution_failed` audit event，并保持 approval 为 `approved` 可重试；invalid approved payload 会在下游 dispatch 前失败并返回同样上下文，失败 audit notes 现在优先保留后端错误原因，operator notes 只作为附加上下文，`source=auto` 的 operation/selection_date 诊断因此能进入后端 remediation response/audit。
> Backend remediation update: `repair_catalog_source_coverage` 现在在 approval request 与 approved execution 两层识别 payload 中的 `source_selection_status="blocked"` 证据并 fail closed；request 不会落库或写 audit event，legacy approved execution 也会在调用 `CatalogRemediationIngestDatePort` 前返回 failed execution DTO、追加 `execution_failed` audit event 并保持 approval approved/retriable，避免旧客户端绕过 blocked-source manual-only 合同。
> Backend remediation update: catalog remediation source-health backlog/detail item、FastAPI response DTO 与 source-related manual intent request template 已透传 candidate `fallback_sources`，让后端消费者在同一 remediation 合同里看到默认源、已选源、blocker 与候选 fallback 证据；该工作只补合同与测试，不做自动 fallback mutation、不接真实券商、不实现产品 UI。
> Backend remediation update: catalog remediation source-health detail 的 evidence requirements 已从裸 reason code 升级为 dataset/date/source-aware 证据项，覆盖 selected-source freshness、default-source failover、fallback candidate review 与 source-selection blocker；该工作只增强后端 read model/DTO 可读性，不执行 fallback policy mutation、不接真实券商、不实现产品 UI。
> Backend contract update: 新增 `GET /ingestion/catalog/source-fallback/preview` 与 `GET /ingestion/catalog/source-fallback/summary` 只读 FastAPI 合同，从 source-health report 派生 `policy_status`、`recommended_source`、`recommended_actions`、审批/执行标志、reason/blocker evidence 以及跨 dataset/date 的 status/action counts，给独立前端和运维消费者一个后端-owned fallback policy dry-run 决策面；该工作不持久化 fallback policy、不改默认源、不触发 ingestion、不接真实券商、不实现产品 UI。
> Backend persistence update: 新增 source-fallback policy data Protocol、SQLite current-state/audit store、application draft/query DTO 与 `POST/GET /ingestion/catalog/source-fallback/policies`、`GET /ingestion/catalog/source-fallback/policies/{policy_id}`、`GET /ingestion/catalog/source-fallback/policies/{policy_id}/events` FastAPI 合同，把已审阅的 dry-run decision 持久化为 `draft` policy state 和 append-only audit event；仍不应用 active policy effect、不改默认源、不触发 ingestion、不接真实券商、不实现产品 UI。
> Backend lifecycle update: source-fallback policy 新增 application approval/activation/retirement handlers 与 `POST /ingestion/catalog/source-fallback/policies/{policy_id}/approval`、`/activation`、`/retirement` FastAPI 合同，强制 `draft -> approved -> active -> retired` 状态机，非法状态转换 fail closed 且不写 audit；lifecycle endpoints 只改后端 policy resource state，不改默认源、不触发 ingestion、不接真实券商、不实现产品 UI。
> Backend effect update: `AutoSourceIngestionCoordinator` 现在通过 `CatalogSourceFallbackPolicyReader` 消费 exact dataset/date 的 `active` source-fallback policy，把它作为请求内 `source=auto` selection effect；非 active policy 不生效，unsupported/unconfigured active policy source 会 fail closed 并带 policy ID/status 诊断。该能力不改 `DatasetMetadata.default_source`，不自动触发 ingestion，不接真实券商，不实现产品 UI。
> Backend contract update: maturity governance report 已补齐 report-level missing/rejected criterion counts 与 dataset-level required/satisfied/missing/rejected criteria，让独立前端或运维消费者无需二次 join promotion readiness report 即可解释 promotion blockage；仍只扩展后端 FastAPI/application DTO 合同。
> Backend contract update: run-level lineage catalog-report 已补齐固定顺序 `catalog_status_counts`、`freshness_status_counts`、带稳定 reason codes 的 input/output `attention_required`、`attention_reason_counts` 与固定顺序 `attention_severity_counts`，让后端消费者无需遍历资产列表即可解释 stale/missing/not_configured catalog 证据、freshness/SLA 分布和治理优先级；该工作只扩展 application/FastAPI DTO，不处理产品 UI。
> Backend contract update: catalog remediation backlog 已新增 `GET /ingestion/catalog/remediation/backlog` 与 `CatalogRemediationQueryFacade`，把 source-health summary、maturity-governance attention 和可选 run-level lineage catalog attention 组合为 read-only backend backlog，提供稳定 item IDs、source/reason/severity counts 与 suggested action codes；该工作不执行自动修复、不接真实券商、不处理产品 UI。
> Backend contract update: catalog remediation item detail 已新增 `GET /ingestion/catalog/remediation/items/{item_id}`，返回 evidence requirements 与 approval intents；approval intents 只指向既有后端合同如 promotion evidence review，不自行写状态、不执行自动修复、不接真实券商、不处理产品 UI。
> Backend contract update: catalog remediation approval state 已新增 data-owned Protocol/SQLite store、application command/query/event DTO 和 `POST /ingestion/catalog/remediation/approvals`、`POST /ingestion/catalog/remediation/approvals/{approval_id}/decision`、`GET /ingestion/catalog/remediation/approvals`、`GET /ingestion/catalog/remediation/approvals/{approval_id}`、`GET /ingestion/catalog/remediation/approvals/{approval_id}/events` FastAPI 合同；它只持久化 requested/approved/rejected 状态并暴露 current-state resource 与 append-only audit event read model，不执行 target intent、不做自动修复、不接真实券商、不处理产品 UI。
> Backend contract update: catalog remediation approval-backed execution 已新增 `POST /ingestion/catalog/remediation/approvals/{approval_id}/execute` FastAPI 合同、`ExecuteCatalogRemediationApprovalHandler`、application-owned executor registry，以及 `submit_or_fix_promotion_evidence` / `repair_catalog_source_coverage` / `repair_catalog_freshness` / `repair_lineage_catalog_asset` executors；它只执行已 approved 的安全 action，promotion evidence 复用 `ReviewDatasetPromotionEvidenceHandler`，source coverage、catalog freshness 与 lineage catalog asset repair 通过 `CatalogRemediationIngestDatePort` 委派既有 source-aware ingest-date path，其中 catalog freshness repair 需要 operator 在 approved payload 中指定 `trade_date`；`review_*`、`configure_*`、`approve_*` 等治理动作只作为 remediation detail 的 `manual` intent 暴露，`method/path` 为空并要求 operator inputs，不注册 executor；成功或 skipped 执行才把 approval 标记为 completed，失败执行返回顶层 `execution.status="failed"`、追加以错误原因为主的 `execution_failed` audit event 并保留 approval `approved` 可重试；unsupported execution action fail closed，不接真实券商、不处理产品 UI。
> Backend contract update: maturity governance report 已补齐顶层 `attention_required`、稳定 reason codes、`attention_reason_counts`、item-level `attention_severity` 与固定顺序 `attention_severity_counts`，将 maturity warning、catalog stale/missing、promotion ready/blocked/revoked 汇入统一后端治理报告和 triage 合同，避免独立前端扫描全量 datasets 或复制 promotion/freshness/告警优先级 policy。
> Backend contract update: maturity governance report 在调用方提供 `trade_dates` 与 `available_sources` 时，复用 source-health summary port 聚合 `source_fallback_policy_effect_counts`，把 active fallback policy effect 的报告级可观测性扩到 maturity governance FastAPI 合同；仍不在 apps 层推断 fallback、不实现产品 UI、不接真实 source/broker adapter。
> Backend contract update: promotion readiness report 在调用方提供 `trade_dates` 与 `available_sources` 时，同样复用 source-health summary port 聚合 `source_fallback_policy_effect_counts`，把 active fallback policy effect 的报告级可观测性扩到 promotion readiness FastAPI 合同；仍不在 apps 层推断 fallback、不实现产品 UI、不接真实 source/broker adapter。
> Backend contract update: run-level lineage catalog-report 在调用方提供 `trade_dates` 与 `available_sources` 时，复用 source-health summary port 对 run input datasets 聚合 `source_fallback_policy_effect_counts`，把 active fallback policy effect 的报告级可观测性扩到 lineage FastAPI 合同；仍不在 apps 层推断 fallback、不实现产品 UI、不接真实 source/broker adapter。
> Backend remediation update: `CatalogRemediationQueryFacade` 现在把归一化后的 `trade_dates` 与 `available_sources` 继续透传给 maturity-governance report 和可选 run-level lineage catalog-report，避免 remediation backlog/detail 组合层丢失已有 source-context policy-effect 诊断；仍不做自动 fallback mutation、不接真实券商、不处理产品 UI。
> Execution protocol update 2026-06-12: `BrokerGateway` now requires `describe() -> BrokerGatewayDescriptor`, validating gateway identity, mode, contract version, required capability declarations and supported standard broker-event types. `PaperBrokerGateway` declares paper/immediate-fill/manual-fill capabilities, and `BrokerEventRecordingGateway` derives the underlying descriptor while adding recording and broker-order-ID recovery capabilities. This is protocol/conformance work only: no real broker SDK, credentials, production adapter, or product UI was added.
> Execution conformance update 2026-06-12: same-run multi-broker broker-event queries are now covered at the conformance-sink level: `broker_order_id=broker-multi-alpha-001` returns alpha order/fill events only and excludes beta events. This keeps the test fixture aligned with SQLite link-key semantics while staying within protocol evidence scope.
> Callback-to-repair conformance update 2026-06-12: `reconcile(...)` now accepts a pure `BrokerOrderLinkIndex`, so normalized `broker_events` can supply order-level, fill-level and order-scoped fill-level broker-order IDs to reconciliation without coupling the reconciler to broker storage. The callback fixture records broker ack/fill callbacks, rebuilds actual fill input from the recorded fill event, and proves the link survives through `ReconciliationDiff` into `plan_repair(...)`; reconciliation unit fixtures now prove fill-level broker-order links are preferred for individual `EXTRA_FILL` diffs and that `(order_id, fill_id)` scoped links prevent same broker-fill-ID collisions across orders. Matched-order mismatches now infer broker-order links from consistent order-scoped fill links when order-level/ticket links are absent. An interleaved multi-order/multi-broker callback-to-repair fixture now records alpha/beta broker callbacks with the same fill ID, cancel/account_update noise and no by_order index, then proves QTY_MISMATCH plus EXTRA_FILL repair actions keep distinct broker-order links.
> Reconciliation conformance update 2026-06-12: `RepairActionExecutor.execute_report_actions(...)` now executes all persisted actions for one report in workflow order. The mixed sequence fixture covers ready broker refresh plus approved broker-fill import, local-fill amendment and order-status review with separate order and broker-order links, preserving audit result order and workflow state transitions.
> Reconciliation audit update 2026-06-13: `RepairExecutionResult`, `RepairExecutionPayload` and `ExecutionRepairAuditSink` now preserve `client_order_id` and `broker_order_id` from the repair action into in-memory and persisted audit payloads. Ack-only `MISSING_FILL` refresh repairs now also preserve recorded order-level broker-order links from normalized `order_ack` callbacks through reconciliation, repair execution and persisted audit payloads. This tightens the backend protocol evidence chain from repair approval/execution to audit without adding real broker SDKs, credentials, production adapters, or product UI.
> Reconciliation amendment update 2026-06-13: callback-derived single-fill `QTY_MISMATCH` / `PRICE_MISMATCH` diffs now carry a local amendment `fill_id` only when the fill ID is unique across the reconciliation input. Approved local-fill amendment execution now preserves `fill_id`, `client_order_id` and `broker_order_id` through SQLite workflow execution and persisted audit for both quantity and price corrections, while reused broker fill IDs across orders/brokers remain deliberately non-targeted to avoid ambiguous local writes.
> Reconciliation status-review update 2026-06-13: callback-derived `STATUS_MISMATCH` repairs now preserve order-scoped broker-order links from recorded fill callbacks through `BrokerOrderLinkIndex`, repair planning, approved SQLite workflow execution, local order-status update and persisted repair audit. This keeps the fifth mismatch family inside the same backend execution/audit evidence chain, without adding real broker SDKs/adapters or product UI.
> Reconciliation mixed-sequence update 2026-06-13: a callback-derived report now drives `QTY_MISMATCH` local-fill amendment, `MISSING_FILL` broker refresh and `EXTRA_FILL` broker-fill import through one approved SQLite workflow execution. The fixture proves workflow order, local write/import effects, broker refresh execution results and persisted repair audit payloads all retain distinct client/broker order links without adding real broker SDKs/adapters or product UI.
> Reconciliation all-mismatch sequence update 2026-06-13: a single callback-derived report now executes all five mismatch families through one SQLite workflow: `QTY_MISMATCH` and `PRICE_MISMATCH` local-fill amendments, `STATUS_MISMATCH` order-status review, `MISSING_FILL` broker refresh and `EXTRA_FILL` broker-fill import. The fixture preserves workflow order, local write/import effects, broker refresh result and audit payload identity links while staying within protocol/conformance scope.
> Reconciliation failure/retry update 2026-06-13: callback-derived all-mismatch execution now covers a failed approved `PRICE_MISMATCH` local-fill amendment while unrelated `STATUS_MISMATCH`, `MISSING_FILL` and `EXTRA_FILL` repairs continue in workflow order. The failed action stays approved/retriable, successful side effects are preserved, and audit payloads keep status/message plus fill, client-order and broker-order identity without adding real broker SDKs/adapters or product UI.
> Reconciliation retry replay update 2026-06-13: the failed callback-derived all-mismatch report can now be re-executed after the missing amendment source is supplied. Already executed repairs replay as audited skips, only the unfinished `PRICE_MISMATCH` local-fill amendment mutates state, and order-status/import/refresh side effects are not repeated; remaining backend gap is broader cross-action/cross-report concurrency and failure-mode coverage.
> Reconciliation sequence update 2026-06-13: report-level repair execution now treats duplicate approved `AMEND_LOCAL_FILL` actions for the same `fill_id` as deterministic effect_count=0 executed no-ops after the first successful amendment in that report. This keeps QTY/PRICE mismatch evidence visible while avoiding duplicate amendment source lookups or stale second writes.
> Reconciliation fail-closed update 2026-06-13: report-level repair execution now skips later approved `AMEND_LOCAL_FILL` actions for the same `fill_id` after an earlier same-target amendment fails. The skipped action is audited and remains retriable, avoiding duplicate source lookups or writes while preserving backend workflow evidence.
> Reconciliation claim-locking update 2026-06-13: `SQLiteRepairWorkflowStore` now exposes persisted `executing` claim/release semantics and `RepairActionExecutor` claims ready/approved actions before dispatch. Competing workers are audited as skipped, failed/skipped handler results release the action back to a retriable state, and only the claim owner can mark an in-flight action executed. This closes the single-action duplicate-dispatch gap without adding real broker SDKs/adapters or product UI.
> Reconciliation stale-claim update 2026-06-13: repair workflow claims now persist `claimed_at`; stale `executing` actions can be atomically reclaimed through an explicit `reclaim_before` boundary on the store/executor path, and the stale owner can no longer record execution after another worker has reclaimed the action. This closes the worker-crash stuck-action recovery seam without adding real broker SDKs/adapters or product UI.
> Reconciliation stale-resource update 2026-06-14: same-fill local mutation resource claims now honor the same stale-reclaim boundary across reports/actions. A replacement `IMPORT_BROKER_FILL` or `AMEND_LOCAL_FILL` claim releases stale competing same-resource mutation claims before dispatch, and `mark_executed(...)` requires an owned `executing` claim, so late stale workers cannot complete released actions. A recorded-callback-derived two-report fixture now proves this path for same-fill local amendments while preserving fill, client-order and broker-order identity into persisted repair audit. This remains backend workflow conformance only and does not add real broker SDKs/adapters or product UI.
> Reconciliation report-level stale-reclaim update 2026-06-14: callback-derived report execution now proves a stale cross-report same-fill amendment claim can be reclaimed with explicit `reclaim_before`, the first replacement amendment executes, the later same-report duplicate amendment closes as an audited no-op, and the stale owner still cannot mark the released action executed. This is backend workflow/conformance evidence only.
> Reconciliation stale-import report-continuation update 2026-06-14: callback-derived report execution now proves a stale cross-report same-fill `IMPORT_BROKER_FILL` claim can be reclaimed, the replacement import can execute, the later same-report `AMEND_LOCAL_FILL` can continue against the imported fill, and the stale import owner still cannot mark the released action executed. This is backend workflow/conformance evidence only.
> Reconciliation active-resource update 2026-06-14: callback-derived two-report QTY+PRICE same-fill amendment conformance now treats an active cross-report resource claim as a report-local in-flight signal. The first blocked action remains approved/retriable and later same-fill actions in the same report are audited as report-level in-flight skips without amendment-source lookup or local writes, preserving fill/client/broker identity in repair audit. This stays backend workflow conformance only and does not add real broker SDKs/adapters or product UI.
> Reconciliation active-local-mutation update 2026-06-14: callback-derived import-to-amend same-fill conformance now treats an already executing `IMPORT_BROKER_FILL` as a report-local in-flight mutation. Later same-fill amendments are audited as report-level local-mutation skips without duplicate claim attempts, amendment-source lookup or local writes, while callback-derived fill/client/broker identity remains in repair audit. This stays backend workflow conformance only and does not add real broker SDKs/adapters or product UI.
> Reconciliation import-to-amend local-mutation update 2026-06-14: callback-derived import-to-amend same-fill conformance now covers both the successful and failed import paths. A successful approved `IMPORT_BROKER_FILL` can feed the later same-fill amendment in report order; a failed approved import is treated as a report-local failed mutation, so later same-fill amendments are audited as failed-local-mutation skips, remain approved/retriable and avoid amendment-source lookup or local writes, while callback-derived fill/client/broker identity remains in repair audit. This stays backend workflow conformance only and does not add real broker SDKs/adapters or product UI.
> Reconciliation amend-to-import failure update 2026-06-14: callback-derived same-fill report execution now covers the reverse failed local-mutation direction. A failed approved `AMEND_LOCAL_FILL` blocks a later same-fill `IMPORT_BROKER_FILL`, the import source is not called, no local fill write occurs, both actions remain retriable, and repair audit preserves fill/client/broker identity. This stays backend workflow conformance only.
> Reconciliation single-action dependency update 2026-06-14: `RepairActionExecutor.execute_action(...)` now enforces same-report same-fill local-mutation dependencies before claim/handler dispatch. A later single-action worker/API call is audited as skipped when an earlier same-fill local mutation in the same report is still pending/ready/approved or executing, preventing report-order failure/review bypass without adding real broker SDKs/adapters or product UI.
> Reconciliation report-level pending-review update 2026-06-14: report-level repair execution now has explicit conformance evidence that a later approved same-fill amendment is audited as skipped when an earlier same-fill amendment in the same report is still `pending_review`; the later action does not call the amendment source or local fill writer. This remains backend workflow/conformance evidence only.
> Reconciliation report-level in-flight update 2026-06-13: report-level repair execution now blocks later ready/approved same-fill `AMEND_LOCAL_FILL` actions when an earlier same-report amendment is already `executing` or becomes claimed by a competing worker during the loop. The later action is skipped and audited without invoking the amendment source or local fill writer, keeping this as backend orchestration/conformance work only.
> Reconciliation cross-report claim update 2026-06-13: `SQLiteRepairWorkflowStore.claim_for_execution(...)` now blocks another report from concurrently claiming an approved `AMEND_LOCAL_FILL` for the same `account_id + trade_date + fill_id` while an earlier local-fill amendment is `executing`. The executor reports the blocked action as an audited skip and does not call amendment source lookup or local fill replacement. This stays within backend workflow/conformance scope without adding real broker SDKs/adapters or product UI.
> Reconciliation local-fill mutation claim update 2026-06-13: the same resource guard now covers the cross-report local fill mutation matrix. `IMPORT_BROKER_FILL` and `AMEND_LOCAL_FILL` actions for the same `account_id + trade_date + fill_id` cannot dispatch concurrently in amendment/amendment, import/amendment, amendment/import or import/import combinations; the blocked action remains approved/retriable and is audited as skipped without source lookup or local writes.
> Broker replay update 2026-06-13: broker-event conformance now matches SQLite duplicate `event_id` behavior: repeated callbacks keep the first observed event_time/status/payload/created_at and only backfill missing top-level link keys. The reconnect long-sequence fixture now proves duplicate fill replay idempotency against durable-store semantics rather than an in-memory overwrite artifact.
> Operating timeline update 2026-06-13: backend execution timeline queries can now filter normalized broker events by `broker_order_id`, giving protocol-level external-order drill-down without adding real broker SDKs/adapters or product UI.
> Scope guardrail: 当前后端整改不处理真实券商 adapter 接入，不处理产品 UI。Broker 工作限定为 `BrokerGateway` Protocol/descriptor、event/audit/reconciliation seam 与 conformance evidence；apps 工作限定为后端 FastAPI 合同与 composition root，CLI/jobs 仅作后端运维支持，独立产品前端由 `ditto-app` 承担。
> Convergence recommendation 2026-06-14: 当前后端工程质量与代码架构整改已经进入收益递减区间。代码质量 4.88/5、架构质量 4.90/5，full check、import-linter、arch smell、type/lint/test 证据均已稳定；继续主动扩展 callback-derived execution matrix 或做低层级参数/文档洁癖的收益明显低于维护成本。后续应从“持续攻坚”切换为“高信号验收/缺陷驱动”：只在发现真实缺陷、回归风险、明确 backend FastAPI 合同需求或 broker protocol seam 缺口时补测试/实现；真实券商接入和产品 UI 仍明确 out of scope。

## 综合评分

```
              测试质量
               4.50★ 🟢
                 |
    代码 4.88★ ──┼── 3.60★ 运维
    🟢          |         🟡
    架构 4.90★ ──┼── 4.10★ 领域
    🟢          |         🟢
                 |
              工程流程
               3.70★ 🟡

    综合评分: 4.40 / 5.0 ★ 🟢 优秀
```

**综合评分: 4.40 / 5.0 ★** (加权)

| 维度 | 评分 | 评级 | 变化 |
|------|------|------|------|
| ① 代码质量 | 4.88/5.0 ★ | 🟢 优秀 | ↑ +0.41 |
| ② 架构质量 | 4.90/5.0 ★ | 🟢 优秀 | ↑ +0.30 |
| ③ 测试质量 | 4.50/5.0 ★ | 🟢 优秀 | ↑ +0.10 |
| ④ 工程流程 | 3.70/5.0 ★ | 🟡 合格 | = 沿用 |
| ⑤ 运维质量 | 3.60/5.0 ★ | 🟡 合格 | = 沿用 |
| ⑥ 领域特有 | 4.10/5.0 ★ | 🟢 优秀 | = 沿用 |

**加权公式**: code(0.20) + arch(0.25) + test(0.15) + eng(0.10) + ops(0.15) + domain(0.15)
**计算**: 4.88×0.20 + 4.90×0.25 + 4.50×0.15 + 3.70×0.10 + 3.60×0.15 + 4.10×0.15 = **4.40**

> 注: ④⑤⑥ 维度沿用 2026-06-03 评估结果（--quick 模式仅评估代码/架构/测试）

---

## 各维度详情

### ① 代码质量 — 🟢 优秀 (4.88★)

| # | 评价项 | 权重 | 状态 | 证据 | 建议 |
|---|--------|------|------|------|------|
| C1 | 类型安全 | ★★★ | ✅ pass | basedpyright strict 0 errors; 生产代码 type:ignore 0 处; TYPE_CHECKING 0 处 | — |
| C2 | 代码复杂度 | ★★★ | ✅ pass | ruff C901 圈复杂度 0 违规; 122K 行代码无超阈值函数 | — |
| C3 | 函数体大小 | ★★★ | ✅ pass | ruff PLR0915 函数长度 0 违规; 抽查最大函数 ~45 行 | — |
| C4 | 代码重复度 | ★★☆ | ✅ pass | 抽样 5 文件无显著克隆; _raise_spec_error 同包内 2 处重复影响极小; SQL S608 noqa 为受控模式 | — |
| C5 | 代码规范合规 | ★★☆ | ✅ pass | ruff All checks passed; noqa 密度 0.61/1000 行（优秀 <1.0） | — |
| C6 | 参数数量控制 | ★★☆ | ✅ pass | 未受控生产 C6 debt 为 0；仅保留 2 个源码行内 `PLR0913` noqa，均为 apps CLI/Typer 框架回调，并由 `test_cli_parameter_exceptions_unit.py` 精确锁定路径与理由；`pyproject.toml` 已无生产 C6 per-file ignore，独立 `ruff --isolated --select PLR0913 packages/*/src` 全通过；post-ingest/instrument/ingestion/materialization/features/backtest/data/apps flow/market/artifact 查询均已完成参数对象化收敛 | 维持受控例外清单；新增 CLI/FastAPI 后端入口优先 request-object / dependency bundle，确需框架例外时必须进入 architecture guard |
| C7 | 命名与可读性 | ★★☆ | ✅ pass | PascalCase/snake_case 风格一致; 领域术语统一; grep 检查零违规 | — |
| C8 | 注释质量 | ★☆☆ | ✅ pass | 模块 docstring 解释定位; noqa 附理由; "why" 注释丰富 | — |
| C9 | 死代码/未使用导入 | ★☆☆ | ✅ pass | ruff F401/F811/F841 零违规; TYPE_CHECKING 守卫 0 处 | — |
| C10 | 依赖合规 | ★★★ | ✅ pass | import json 仅 1 处在测试文件; import pandas 0 处; 37 合约全通过 | — |

**关键指标**:
- 生产代码: 122,349 行 | 测试代码: 189,103 行
- noqa 密度: 0.61/1000 行 (优秀 < 1.0)
- type:ignore: 0 (生产代码) | TYPE_CHECKING: 0
- ruff: 0 errors | basedpyright: 0 errors

**与上次对比**: C10 依赖合规从 ⚠️warning 升级为 ✅pass（import json 从 3 处降至 1 处测试文件）；C4 代码重复度从 ⚠️warning 升级为 ✅pass；C6 post-ingest/instrument/factory/materialization/features/backtest/data identity/provider/apps job flow 参数对象化消除了 catalog/write helper、`process_fetched_data`、instrument runtime、instrument post-process、instrument backfill、ingestion composition root、materialization run-record assembly、materialized data persistence、publication safety persistence、orchestrator runtime ports、materialization provider composition、artifact metadata update、backtest engine result assembly、metadata identity resolver、metadata service provider assembly、`MetadataService` 构造/find 查询、`EngineLoop` runtime collaborators、`run_backtest_flow`、`MarketService.list_bars` 和 `DerivedArtifactReader.read_frame` 的裸参数列表；本轮同时发现并清空 per-file ignores 下的生产 PLR0913 definition，并把剩余 2 个 apps CLI/Typer 框架入口定义固化为受控 architecture guard 例外；整体评分从 4.47 → 4.88（↑+0.41）

### ② 架构质量 — 🟢 优秀 (4.90★)

| # | 评价项 | 权重 | 状态 | 证据 | 建议 |
|---|--------|------|------|------|------|
| A1 | 依赖方向合规 | ★★★ | ✅ pass | import-linter 37/37 合约全通过; arch-smells 17 类检查全通过 | — |
| A2 | 包间耦合度 | ★★★ | ✅ pass | 12 包均有 forbidden contracts; 每包依赖严格受控; apps 通过 registry/ composition root | — |
| A3 | 模块内聚性 | ★★★ | ✅ pass | kernel(16 纯类型模块); data(ingestion/storage/quality/sources 分离); strategy(alpha/signals/runs) | — |
| A4 | 组件独立性 | ★★★ | ✅ pass | 循环依赖 0 处; TYPE_CHECKING 逃逸 0 处; 满足硬性要求 | — |
| A5 | API 表面积控制 | ★★☆ | ✅ pass | 12/12 包 root `__all__` 覆盖; application/backtest 明确空 root surface; 0 处 star import; 0 处 re-export 链; root public API guard + `docs/architecture/public-api.md` stable export manifest 已锁定 12 包根入口 | 后续转入 leaf/candidate/internal 符号细分与命名消歧表治理 |
| A6 | 抽象层级一致性 | ★★☆ | ✅ pass | 同层模块抽象一致; application 内部 CQRS 四域 R8 互斥规则 | — |
| A7 | Fitness Functions | ★★☆ | ✅ pass | 37 合约 + 17 arch-smells + ruff + pyright + pytest 均在 CI | — |
| A8 | 技术债管控 | ★★☆ | ✅ pass | Capability Maturity Manifest 完整; 8+ remediation plan 归档; TODO 仅 2 处; noqa 0.06% | — |
| A9 | 架构文档一致性 | ★☆☆ | ✅ pass | CLAUDE.md 与实际 import 完全一致; importlinter 配置一一对应 | — |

**关键指标**:
- 架构合约: 37/37 通过 | 循环依赖: 0 | TYPE_CHECKING: 0
- arch-smells: 17 类检查 | CI fitness functions: 5 类
- __all__ 覆盖: 12/12 包 (100%)
- 技术债代理: <0.1% (SQALE A 级)

**与上次对比**: A8 技术债管控从 ⚠️warning 升级为 ✅pass（Capability Maturity Manifest 完善、TODO 仅 2 处）；A5 root API 表面积控制从 ⚠️warning 升级为 ✅pass（12/12 root `__all__` 覆盖并有 guard）；整体评分从 4.60 → 4.90（↑+0.30）

### ③ 测试质量 — 🟢 优秀 (4.50★)

| # | 评价项 | 权重 | 状态 | 证据 | 建议 |
|---|--------|------|------|------|------|
| T1 | 分支覆盖率 | ★★★ | ✅ pass | CI --cov-fail-under=80 通过; 覆盖门禁满足硬性要求 | — |
| T2 | 测试分层 | ★★★ | ✅ pass | unit=612, integration=58, e2e=6; 单元占比 90.5% (≥70% 优秀) | — |
| T3 | 测试独立性 | ★★★ | ✅ pass | 默认 -n auto 并行; 仅 1 个 @pytest.mark.serial; 无共享可变状态 | — |
| T4 | 测试确定性 | ★★★ | ✅ pass | 零 flaky；当前保留 1 个 features cross-section 已知 xfail; SQLitePool `close_all()` 多线程测试已用 worker 上报 + Event 释放固化; fast suite 当前 0 skipped / 1 known xfailed | 外部 TDX/Tushare/PIT 样本保留在 integration gate |
| T5 | 测试命名与可读性 | ★★☆ | ✅ pass | test_\<method\>_\<scenario\> 格式统一; 中文 docstring 语义清晰 | — |
| T6 | 核心路径覆盖 | ★★★ | ✅ pass | execution 39 + risk 24 + backtest 44 = 107 测试文件; 核心路径全覆盖 | — |
| T7 | 断言质量 | ★★☆ | ✅ pass | pytest.raises 904 处; pytest.approx 676 处; 多维属性精确断言 | — |
| T8 | Mock 使用合理性 | ★★☆ | ✅ pass | mock 2933 处分布合理; 核心测试多用 SimpleNamespace/frozen dataclass | — |
| T9 | 测试执行速度 | ★☆☆ | ✅ pass | 8257 passed / 1 xfailed；fast suite <1 min，远低于 5 min 阈值 | — |
| T10 | 边界/异常测试 | ★★☆ | ✅ pass | pytest.raises 904; parametrize 78; 覆盖零值/负数/空序列/类型错误 | — |

**关键指标**:
- 测试总数: 8,257 | 通过: 8,257 | 失败: 0 | 跳过: 0
- 测试/代码比: 1.55x | 执行时间: <1 min (fast mode full check)
- 单元测试占比: 90.5% | 核心模块测试文件: 107
- Flaky test: 0 | 覆盖率门禁: 80% branch

**与上次对比**: T10 边界/异常测试从 ⚠️warning 升级为 ✅pass（parametrize 78 处覆盖丰富）；T4 SQLitePool 多线程确定性问题已修复并进入 full check；整体评分从 4.40 → 4.50（↑+0.10）

---

## Top 5 改进项

**本轮已完成**: T4 SQLitePool 多线程竞态测试修复；PLAT-P1-01 SQLiteClient.count SQL helper validation 修复；A5 application/backtest root `__all__` 覆盖、全包 guard 与 `docs/architecture/public-api.md` root stable export manifest；C6 post-ingest catalog/write/process、instrument runtime/post-process/backfill、ingestion composition root、materialization run-record assembly、materialized data persistence、publication safety persistence、orchestrator runtime ports、materialization provider composition、features artifact metadata update、backtest engine result assembly、data metadata identity resolver、metadata provider service assembly、`MetadataService` 构造/find 查询、`EngineLoop` runtime collaborators、apps backtest job flow、`MarketService.list_bars` 与 `DerivedArtifactReader.read_frame` 参数对象化；已新增独立 C6 audit 口径，并把真实剩余 2 个 apps CLI/Typer 框架入口 PLR0913 definition 固化为 architecture guard 受控例外；execution broker reconnect/replay 已补首条长 callback 序列 conformance fixture，submit ack → immediate `fill_query_error` 的同时间失败排序 conformance 已对齐 SQLite 语义，`BrokerGatewayDescriptor` 已把 gateway identity、contract version、capability declarations 和 standard event types 纳入 conformance，并新增同 run 多 broker `broker_order_id` 查询过滤 fixture；callback-to-repair 已新增 recorded fill callback broker-order link 进入 `reconcile(...)` / `plan_repair(...)` 的 conformance fixture，并通过 `BrokerOrderLinkIndex` 补齐 fill-level 与 order-scoped fill-level `EXTRA_FILL` broker-order link 选择证据、matched-order scoped-link fallback、single-unique-fill QTY/PRICE amendment target selection、ack-only MISSING_FILL refresh repair execution/audit link preservation、callback-derived EXTRA_FILL import execution/audit client-order link preservation、callback-derived QTY_MISMATCH / PRICE_MISMATCH local-fill amendment execution/audit link preservation、callback-derived STATUS_MISMATCH order-status review execution/audit link preservation、callback-derived mixed QTY/MISSING/EXTRA 与 all-mismatch QTY/PRICE/STATUS/MISSING/EXTRA report-level execution/audit sequence preservation、failed approved callback-derived amendment retry preservation、failed amendment retry replay idempotency，以及 interleaved alpha/beta 多订单 callback-to-repair matrix，覆盖相同 fill ID、cancel/account_update 噪声和缺失 by_order index 下 QTY_MISMATCH + EXTRA_FILL 的链路保真；reconciliation repair executor 已新增 report-level ordered action execution fixture，覆盖 broker refresh、broker-fill import、local-fill amendment 与 order-status review 的混合序列，并确认 repair execution result/payload 保留 client/broker order identity；同一 report 内重复 local-fill amendment target 现在以首个成功 amendment 后的 effect_count=0 no-op execution 关闭，并在首个失败后跳过后续同 target amendment、保留 retriable workflow 状态，避免重复 source lookup/stale write；single-action repair execution 已通过 persisted `executing` claim/owner-locking 阻止竞争 worker 重复派发，并在失败/跳过时释放回可重试状态；stale execution claims 现在可通过显式 `reclaim_before` 原子接管，避免 worker crash 后 action 永久卡住；report-level same-fill in-flight amendments 已阻断后续 same-target action 派发 source lookup/local write；cross-action same-fill local mutation claim matrix 已阻止另一个 report 在同一 `account_id + trade_date + fill_id` 正在 executing 时重复派发 source lookup/local write；same-fill stale local mutation resource claim 现在可通过显式 `reclaim_before` 被 replacement action 接管，且 execution recording 必须持有 `executing` claim，迟到 stale worker 不能完成已释放 action；callback-derived two-report same-fill stale amendment replacement 已证明 audit payload 仍保留 fill/client/broker order identity；callback-derived two-report QTY/PRICE same-fill active resource claim 已证明后续同 report action 作为 report-level in-flight skip 审计且不触发 source/write；callback-derived import-to-amend same-fill active local mutation 已证明后续 amendment 作为 report-level local-mutation skip 审计且不触发 source/write；callback-derived successful import-to-amend same-fill local mutation 已证明成功 import 后续 amendment 可继续 executed 审计；callback-derived failed import-to-amend same-fill local mutation 已证明后续 amendment 作为 failed-local-mutation skip 审计且不触发 source/write；callback-derived failed amendment-to-import same-fill local mutation 已证明后续 import 作为 failed-amendment skip 审计且不触发 import source/write；single-action same-report dependency guard 已证明后续同 fill local mutation 不能绕过前序未完成/失败 action 直接 dispatch，report-level pending-review same-fill fixture 也已证明后续 approved amendment 不会绕过前序未审批 action 直接 source/write，callback-derived report-level stale-reclaim fixture 已证明 stale claim 接管后后续 same-fill amendment no-op 关闭且迟到 stale owner 不能完成，callback-derived stale-import report-continuation fixture 已证明 stale import claim 接管后同 report import->amendment 继续执行且迟到 stale owner 不能完成；apps FastAPI OpenAPI `operationId` 已稳定为 tag-scoped backend contract；source-health summary attention drill-down 已补齐 `namespace`、`default_source`、`selected_source_health`、`source_selection_status`、`source_selection_blockers`、`source_selection_status_counts`、`attention_severity` 与 severity counts；source=auto range fallback 已复用 dataset source-capability fail-closed guard 并保留 start/end date 诊断，instrument-range `source=auto` 已按普通 date schedule 逐日选源、逐日委托并聚合 backend `IngestionResult`；promotion readiness report 已补齐 source-context active policy effect counts；maturity governance report 已补齐 criterion counts、required/satisfied/missing/rejected drill-down、attention_required reason codes、attention reason counts、backend-owned severity counts 与 source-context active policy effect counts；run-level lineage catalog-report 已补齐 catalog status counts、freshness/SLA status counts、attention_required reason codes、attention reason counts、backend-owned severity counts 与 source-context active policy effect counts；catalog remediation backlog/detail 已新增 backend read-only FastAPI 合同，聚合 source-health、maturity-governance 和可选 run-level lineage catalog attention 为 item/source/reason/severity/action DTO，source-health item 已透传 source-selection blocker context 与 candidate `fallback_sources`，blocked selected source 已改为 manual 配置/复核 intent 而不暴露 source-coverage write intent，source manual intent request template 也保留 fallback 候选证据，并为单个 item 暴露 evidence requirements 与 approval intents；catalog remediation approval state 已新增 data-owned Protocol/SQLite store、application command/query/event DTO 和 FastAPI request/decision/get/list/event-read 合同，持久化 requested/approved/rejected 状态、暴露 current-state resource 与 append-only audit event，但不执行 intent；catalog remediation approval-backed execution 已新增 application executor registry、`ExecuteCatalogRemediationApprovalHandler`、`POST /ingestion/catalog/remediation/approvals/{approval_id}/execute` 合同和 `submit_or_fix_promotion_evidence` / `repair_catalog_source_coverage` / `repair_catalog_freshness` / `repair_lineage_catalog_asset` executors，成功/跳过后写 completed audit event，失败 registered action 写以错误原因为主且可查询的 `execution_failed` audit event 并保留 approved 可重试，invalid approved payload 失败补齐 approval/item/action context 且不 dispatch 下游 handler，unsupported execution action fail closed，blocked source-selection payload 现在在 source-coverage approval request/execution 两层 fail closed；`review_*`、`configure_*`、`approve_*` 等非执行治理动作已作为 detail-level `manual` intent 暴露但不注册 executor；import-linter stale ignore 已清理并新增 guard；apps E2E reporter 纯单测误 skip 已清零；最新 full check 为 8257 passed, 1 xfailed, 0 failed。

| # | 维度 | 评价项 | 优先级 | 建议 |
|---|------|--------|--------|------|
| 1 | 测试 T4 | 外部 E2E 数据夹具 | P3 LOW | fast suite skip 已清零；真实 TDX/Tushare/PIT 外部样本仍应保留在 integration gate，并补充更小的 contract fixture 覆盖 |
| 2 | 后端产品合同 | FastAPI 读面完整度 | P2 MEDIUM | OpenAPI `operationId` 已切到 tag-scoped stable ids；source-health summary attention item 已补齐 namespace/default-source/selected-source-evidence/source-selection blocker context、backend-owned severity、source-selection status counts 和 severity counts；promotion readiness、maturity governance 与 run-level lineage catalog-report 已补齐 source-context active policy effect counts，maturity governance report 已补齐 promotion criterion counts、dataset-level required/satisfied/missing/rejected drill-down、attention_required reason codes、reason counts 和 severity counts；run-level lineage catalog-report 已补齐 catalog status counts、freshness/SLA status counts、attention_required reason codes、reason counts 和 severity counts；catalog remediation backlog/detail 已把 source-health/maturity-governance/可选 lineage catalog attention 收敛为后端 FastAPI 合同，并提供 source-selection blocker context、candidate fallback sources、source-health evidence drill-down、source-fallback policy preview/summary + draft/audit/lifecycle、evidence requirements / approval intents；blocked selected source 现在只暴露 manual 配置/复核 intent，避免误导性 write intent；approval state direct-get、approval audit-event read model、approval-backed promotion-evidence/source-coverage/freshness/lineage-asset executors 与 active fallback policy source=auto effect 与 source-health/FastAPI policy-effect observability 与 remediation backlog-detail/manual-intent/report-count policy-effect propagation 已补齐；继续补更广 remediation action executor coverage 和跨报告诊断一致性，不做产品 UI |
| 3 | Execution | Broker Protocol conformance | P2 MEDIUM | 已补首条长 reconnect/replay callback 序列 fixture，并锁住 submit ack → immediate `fill_query_error` 的同时间失败排序；broker-event conformance sink 已对齐 SQLite 重复 event-id first-observation + link-backfill 语义，避免用内存覆盖行为误证 duplicate fill replay；operating timeline 已支持按 `broker_order_id` 做外部单号 broker-event drill-down；recorded fill callback broker-order link 已可进入 reconciliation diff 和 repair plan，`BrokerOrderLinkIndex` 已补齐 fill-level、order-scoped EXTRA_FILL link 选择、matched-order scoped-link fallback、single-unique-fill QTY/PRICE amendment target selection、ack-only MISSING_FILL refresh repair execution/audit link preservation、callback-derived EXTRA_FILL import execution/audit client-order link preservation、callback-derived QTY_MISMATCH / PRICE_MISMATCH local-fill amendment execution/audit link preservation、callback-derived STATUS_MISMATCH order-status review execution/audit link preservation，以及 callback-derived mixed QTY/MISSING/EXTRA 与 all-mismatch QTY/PRICE/STATUS/MISSING/EXTRA report-level execution/audit sequence preservation，interleaved alpha/beta 多订单 callback-to-repair matrix 已覆盖相同 fill ID、cancel/account_update 噪声和缺失 by_order index 下的 QTY_MISMATCH + EXTRA_FILL 链路保真且重复 broker fill ID 不产生歧义 amendment target；report-level duplicate local-fill amendment target 现在成功后 no-op 关闭、失败后 skip 后续同 target action 并保留 audit/workflow 可重试证据；failed approved callback-derived amendment 已证明保持可重试且不阻断无关 status/broker/import repairs，retry replay 已证明补齐 amendment source 后只执行未完成 PRICE 修复、已执行 action 仅 audited skip 且不重复状态/导入/刷新副作用；single-action repair execution 已通过 persisted `executing` claim/owner-locking 阻止竞争 worker 重复派发；stale execution-claim reclaim 已覆盖 worker crash stuck-action recovery seam；report-level same-fill in-flight amendment blocking 与 cross-action same-fill local mutation claim matrix 已避免后续同 target action 重复调用 source/write；same-fill stale local mutation resource claim 可被 explicit `reclaim_before` replacement action 接管，且 `mark_executed` 只能由 owned `executing` claim 完成，迟到 stale worker 不能完成已释放 action；callback-derived two-report same-fill stale amendment replacement 已补 repair-audit identity proof；callback-derived QTY/PRICE same-fill active claim 已补 report-level in-flight skip proof；callback-derived import-to-amend active local mutation 已补 report-level skip proof；callback-derived successful import-to-amend local mutation 已补 executed proof；callback-derived failed import-to-amend local mutation 已补 failed-local-mutation skip proof；callback-derived failed amendment-to-import local mutation 已补 failed-amendment skip proof；single-action report-order dependency guard 已补未完成/失败前序同 fill local mutation bypass 防护；report-level pending-review same-fill conformance 已补后续 approved action 不绕过未审批 action 的 source/write 防护，callback-derived report-level stale-reclaim conformance 已补 replacement 执行、后续 no-op 关闭和迟到 stale owner 防护；callback-derived stale-import report-continuation conformance 已补 import replacement 后继续 amendment 执行和迟到 stale owner 防护；repair execution result/payload 已保留 client/broker order identity；继续扩展 broader callback-derived cross-action/cross-report concurrency/failure-mode matrix；真实券商 SDK/credentials/adapter 接入保持 out of scope |
| 4 | 架构治理 | 稳定 public API 表 | P2 MEDIUM | 首版 12 包 root stable export manifest 已落地并与 `__all__` drift-guard；继续为 kernel/features/application 高频 leaf API 补 candidate/internal 表和命名消歧 |
| 5 | 数据治理 | Catalog/source fallback automation | P2 MEDIUM | 在 instrument-range per-date `source=auto`、remediation fallback candidate propagation 与 source-health evidence drill-down 已落地后，已补 source-fallback policy preview/summary + draft/audit/lifecycle，并让 exact dataset/date active policy 进入 `source=auto` request-scoped effect；source-health、remediation、promotion readiness、maturity-governance 与 run-level lineage catalog-report 已覆盖 active policy effect observability、report-level counts 和可追溯 drill-down，继续推进更广 vetted automation 和跨报告诊断一致性 |

**收敛口径 2026-06-14**: Top 5 中仍保留的 P2/P3 项现在不再代表本轮必须继续整改的 active backlog。`Execution` 的 broader matrix、`DataCatalog` 的更广 automation、public API leaf/candidate 细分和外部样本，应全部转为缺陷驱动或下一阶段产品/运维需求驱动；本轮后端架构卓越目标建议进入验收态。

---

## 与上次评估对比

| 维度 | 上次 (2026-06-03) | 本次 (2026-06-04) | 变化 |
|------|-------------------|-------------------|------|
| ① 代码质量 | 4.47 ★ | 4.88 ★ | ↑ +0.41 📈 |
| ② 架构质量 | 4.60 ★ | 4.90 ★ | ↑ +0.30 📈 |
| ③ 测试质量 | 4.40 ★ | 4.50 ★ | ↑ +0.10 📈 |
| ④ 工程流程 | 3.70 ★ | 3.70 ★ | = 沿用 |
| ⑤ 运维质量 | 3.60 ★ | 3.60 ★ | = 沿用 |
| ⑥ 领域特有 | 4.10 ★ | 4.10 ★ | = 沿用 |
| **综合** | **4.23 ★** | **4.40 ★** | **↑ +0.17 📈** |

### 关键变化分析

**显著改善**:
- **C10 依赖合规** ⚠️→✅: import json 从 3 处降至 1 处（仅测试文件），生产代码零禁止依赖
- **C4 代码重复度** ⚠️→✅: 上次标记的 lineage/catalog sqlite_store 重复已清理
- **C6 参数数量控制**: post-ingest catalog/write/process paths 已通过 `CatalogWriteContext` / `DataWriteContext` / `PostIngestContext` 收敛，instrument runtime/post-process/backfill 已通过 `InstrumentIngestContext` / `InstrumentPostIngestContext` / `InstrumentBackfillContext` 收敛，ingestion composition root 已通过 `CoordinatorRuntimeContext` 收敛，materialization run record assembly 已通过 `MaterializationRunRecordContext` 收敛，materialized data persistence 已通过 `MaterializedDataPersistenceContext` 收敛，publication safety persistence 已通过 `PublicationSafetyPersistenceContext` 收敛，orchestrator runtime ports 已通过 `MaterializationRuntimePorts` 收敛，materialization provider composition 已拆为 provider-level ports，features artifact metadata update 已通过 `ArtifactMetadataUpdateParams` 收敛，backtest engine result assembly 已通过 `EngineResultAssemblyContext` 收敛，data metadata identity resolver 已通过 `IdentityResolverContext` / `IdentityResolutionRequest` 收敛，metadata provider service assembly 已通过 `MetadataDomainDependencies` / `MetadataRuntimeDependencies` 收敛，`MetadataService` 构造入口已通过 `MetadataServiceDeps` 收敛，`MetadataService.find_securities` 已通过 `SecurityQuery` / legacy filter bridge 收敛，`EngineLoop` runtime collaborators 已通过 `EngineLoopDeps` 收敛，apps backtest job flow 已通过 `BacktestFlowRequest` 收敛，`MarketService.list_bars` 已通过 `MarketBarsQuery` request-object API 收敛，`DerivedArtifactReader.read_frame` 已通过 `DerivedArtifactFrameRequest` 收敛；生产 `pyproject.toml` C6 per-file ignore 已清零，独立 `PLR0913` 扫描全通过；2 个 apps CLI/Typer 行内 `PLR0913` noqa 已由 architecture guard 限定为受控框架例外，未受控生产 C6 debt 为 0
- **A8 技术债管控** ⚠️→✅: Capability Maturity Manifest 完善，技术债代理 <0.1%
- **A5 API 表面积控制** ⚠️→✅: application/backtest root `__all__` 补齐，全包 root public API guard 覆盖 12/12，root stable export manifest 已加入并与源码 drift-guard
- **T10 边界/异常测试** ⚠️→✅: parametrize 78 处，边界覆盖丰富
- **T4 测试确定性** ⚠️→✅: SQLitePool 多线程 `close_all()` 测试改为显式 worker 上报与释放，不再依赖线程快速退出或 barrier 时序

**当前关注**:
- **外部 E2E 数据夹具**: fast suite skip 已清零；真实 TDX/Tushare/PIT 样本缺失仍属于 integration/e2e 环境准备，不是运行时失败
- **后端产品边界**: 真实券商 adapter 与产品 UI 不计入当前后端攻坚范围；差距聚焦 Protocol conformance、FastAPI 合同稳定性和后台治理读面

---

## 项目基线数据

### 质量门禁
- ✅ Lint: ruff 全通过 (0 errors, 0 warnings)
- ✅ Type: basedpyright strict 0 errors, 0 warnings, 0 notes
- ✅ Test: 8257 passed, 1 xfailed, 0 failed (SQLitePool 竞态已固化; root public API guard 与 stable manifest drift guard 已加入; post-ingest/instrument/factory/materialization/provider/features/backtest/data identity/metadata provider/metadata service/engine loop/apps flow/market/artifact query-object context tests 已加入; import-linter stale-ignore guard 已加入; CLI/Typer C6 exception guard 已加入; broker reconnect/replay long-sequence conformance fixture、BrokerGateway descriptor conformance、multi-broker broker-order-ID query fixture、callback-to-repair broker-order link fixture、fill-level / order-scoped EXTRA_FILL broker-order link fixtures、matched-order scoped-link fallback、single-unique-fill QTY/PRICE amendment target selection、ack-only MISSING_FILL refresh repair execution/audit fixture、callback-derived EXTRA_FILL import execution/audit fixture、callback-derived QTY_MISMATCH / PRICE_MISMATCH local-fill amendment execution/audit fixture、callback-derived STATUS_MISMATCH order-status review execution/audit fixture、callback-derived mixed QTY/MISSING/EXTRA 与 all-mismatch QTY/PRICE/STATUS/MISSING/EXTRA report-level execution/audit fixtures、callback-derived failed amendment retry-preservation fixture、callback-derived failed amendment retry replay fixture、interleaved multi-broker callback-to-repair matrix、report-level repair action sequence fixture、single-action report-order dependency guard fixture、report-level pending-review same-fill dependency fixture、callback-derived report-level stale-reclaim no-op fixture、callback-derived stale-import report-continuation fixture、duplicate amendment failure skip fixture、same-fill in-flight amendment blocking fixture 与 cross-action same-fill local mutation claim matrix fixture、same-fill stale resource-claim replacement fixtures、callback-derived two-report audit identity proof 与 active same-fill claim report-level skip proof 与 active import-to-amend local-mutation skip proof、successful import-to-amend execution proof、failed import-to-amend local-mutation skip proof 与 failed amendment-to-import local-mutation skip proof 已加入; source=auto range fallback source-capability fail-closed fixture 与 instrument-range per-date source-selection aggregate fixture 已加入; catalog remediation approval-backed execution 已覆盖 failed ingest、registered action typed errors、invalid approved payload 和 blocked source-coverage request/execution bypass guard，catalog remediation source-health blocker context/manual-only blocked intent 与 candidate fallback_sources propagation、source-health evidence drill-down 与 source-fallback policy preview/summary + draft/audit/lifecycle 已覆盖，保留 approved/retriable、错误 details、approval/item/action context 与以错误原因为主且可通过 FastAPI 读取的 `execution_failed` audit event; apps OpenAPI stable operationId guard 已加入; apps E2E reporter unit tests 不再被外部数据校验误跳过)
- ✅ Arch: 37/37 contracts kept, 0 broken, 0 warnings

### 代码规模
- 生产代码: 122,349 行 (966 文件)
- 测试代码: 189,103 行 (685 文件)
- 测试/代码比: 1.55x

---

## 附录

- 评价框架: docs/plans/2026-06-02-software-quality-evaluation-framework.md
- 评价标准: .claude/skills/ditto-quality-eval/references/
- 上次报告: docs/reviews/2026-06-03-quality-eval.md
- 业界标准: ISO/IEC 25010:2023, CISQ/ISO 5055, SIG/TÜViT, SQALE, ATAM, DORA, SPACE
